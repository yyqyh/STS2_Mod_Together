# Together（共生体）Mod — 架构与实现说明 · 问题解法清单

> 适用版本：together 0.1.0 / 游戏 v0.111.0 / RitsuLib 0.6.2 / Godot 4.5.1 + .NET 9
> 本文档 = 架构说明 + 已知问题与解法清单（原《问题与解法清单.md》的内容已并入第 5 节）。
> 补丁挂点、函数名与当前源码一致；每段都写了**为什么必须这样做** —— 这里踩过的坑几乎全部来自本体的隐式约定。

---

## 0. 一句话目标

**2~4 名玩家共用一个身体**（人数上限在设置里填）：血量 / 最大生命 / 格挡 / 状态（能力）/ 卡组 /
抽牌堆 / 弃牌堆 / 消耗堆是**同一份**；**手牌与能量各人各一份**。
多人选人界面里想参加的人各按一次「确定参加共生体」按钮即成组（≥2 人），没按按钮的玩家照常各玩各的。

---

## 1. 运行时模型

### 1.1 引擎给的前提（决定了整体设计）

| 事实 | 出处 | 后果 |
|---|---|---|
| `CombatState.Players` 由 `PlayerCreatures.Select(c => c.Player)` 反推 | `CombatState.cs` | 每个 `Player` **必须**有自己的 `Creature`，"一个身体"只能靠**镜像**实现 |
| 牌堆是挂在 `Player` / `PlayerCombatState` 上的实例 | `PlayerCombatState.AllPiles = {Hand, Draw, Discard, Exhaust, Play}` | 共享牌堆只能靠**把回声的字段换成锚点那一份**实现 |
| `CardModel.Pile => _owner?.Piles.FirstOrDefault(p => p.Cards.Contains(this))` | `CardModel.cs` | 牌的 **owner 必须与它实际所在的堆自洽**，否则摘除/查找会静默失败 |
| 界面（卡组按钮 / 卡组界面 / 战斗牌堆控件）**缓存 `CardPile` 引用** | `NTopBarDeckButton` 等 | 只重定向 getter 无效，必须替换 backing field |
| `Player._runPiles` / `PlayerCombatState._piles` 是"首次访问即固化"的数组 | `Player.cs` / `PlayerCombatState.cs` | 换字段后必须把缓存置 `null` |
| `ListExtensions.StableShuffle` 先 `Sort()` 再打乱；`UnstableShuffle` 是裸 Fisher-Yates | `Core/Extensions/ListExtensions.cs` | **两者的结果都依赖输入顺序**，而共生体下两端顺序天然不同（见 4.5） |
| `CardPileCmd.Shuffle` 里 `list.AddRange(drawPileCards)` 枚举的是 `HashSet<CardModel>` | `CardPileCmd.cs` | `CardModel` 没重写 `GetHashCode`（引用哈希）→ 两端枚举顺序必然不同 |
| **.NET 泛型共享**：引用类型实参的泛型方法只生成一份代码 | CLR 行为 | **不能 patch `UnstableShuffle<T>` / `StableShuffle<T>`**：补丁会连带命中 `RelicModel` / `MapPointType` 等所有实例（见 F22） |
| 工作流会把状态写进存档、读档重建 `Player`/`Creature`/`RunState` | `RunState.FromSerializable` | 任何"开局加一次"的操作都必须**幂等**，否则读档滚雪球 |

### 1.2 两种角色：锚点（anchor）与回声（echoes）

- **锚点**：权威实例持有者。持有一切"只有一份"的东西 —— 主卡组 `Player.Deck`、四个战斗牌堆、身体真实数值（HP/Block/Powers）。
- **回声**（可多位）：访问入口被重定向到锚点，但**手牌与能量保持独立**。
- **谁是锚点**：`RunState.Players` 顺序里最靠前的成员。两端必须算出同一个答案，而 `Players` 顺序来自大厅、两端天然一致，并写进存档。
- **组内人数**：`SymbiosisMembers` 里"确定"过的人数（≥2，最多 = 设置里的"共生体人数上限"，2~4）。
- 注册表：`Core/Multiplayer/TogetherPair.cs`；静态字段 `_anchor / _echoes / _armedRunState / _anchorCombatState`。
  判定一律走 `IsAnchor / IsEcho / IsMember / Members / OthersOf`（**比较对象引用**）。镜像类逻辑一律"推给组里**其他所有**成员"。

### 1.3 共生体成员（谁和谁配对）

`Core/Multiplayer/SymbiosisMembers.cs`：选人界面按过「确定参加共生体」的玩家集合。

- **主机权威**：客户端只能发"我想确定 / 取消"的请求，主机校验（只能确定自己 + 不超额）后落库并广播。
- 状态里存的是 `netId` 字符串；配对时与 `RunState.Players` 求交集 → 得到锚点 / 回声。
- **客户端请求链路依赖 `RunManager.Instance.NetService`**（RitsuLib 的 `OnRequestMessage` 就是拿它判"我是不是主机"），
  所以选人界面阶段必须提前把它绑上，见 4.2 与 F20。

---

## 2. 生命周期

```
大厅（选人界面）
  ├─ 设置开关（主机权威，sidecar 同步）
  ├─ 把大厅的 NetService 提前绑到 RunManager（否则主机收不到共生体请求）
  ├─ 「确定参加共生体」按钮：本地玩家按一次 → 请求主机 → 广播成员集合
  └─ 起程门控：只有 1 个人确定时禁用起程按钮
        ↓
开局（RunState 工厂返回之后）
  └─ TogetherPair.Arm(runState, isNewRun)
       ├─ 取"确定过的成员" → _anchor / _echoes（读档时按种子找回，见 F19）
       ├─ 合并初始卡组（开关开启、仅 isNewRun；搬运式，幂等）
       ├─ 血量上限提升（仅 isNewRun；写进存档后不再重复）
       ├─ DeckField(echo) = anchor.Deck，清 RunPileCache(echo)
       └─ 进阶之灾去重 + 金币对齐（GoldMirror.OnArm）
        ↓
战斗开始
  ├─ PlayerCombatState 构造 → OnCombatStateCreated：回声 adopt 锚点四口牌堆 + 清缓存 + BodyMirror.SyncAll
  ├─ Player.PopulateCombatState：只让锚点跑（否则同一套主卡组被克隆两遍）
  └─ 初始洗牌前按序列化键排序（DeterministicInitialShufflePatch）
        ↓
战斗中
  ├─ 牌堆 getter（回声 → 锚点）＋ backing field 替换
  ├─ 归属规则：在手牌归手牌主人 / 落进共享堆归锚点 / 另记"最后持有者"供钩子还原
  ├─ 洗牌与比较器保证两端取到同一张牌
  ├─ 能力镜像 + 回合族钩子只算一次
  └─ 牌堆计数 / 卡组按钮 UI 与数据同步
        ↓
读档 / 重连：FromSerializable → Arm(isNewRun: false)（不重复加血、不重复合卡组）
```

---

## 3. 目录与职责

| 文件 | 职责 |
|---|---|
| `Const.cs` | ModId / 版本 / 资源路径常量（ModId 必须与清单、DLL、PCK 三者一致） |
| `Main.cs` | 入口：建 logger、交给 RitsuLib 自动注册、**逐类**安装 Harmony 补丁（打 `applied=xx failed=xx` 日志） |
| `Core/Config/TogetherSettings.cs` | 设置模型（共生体开关 / 合并初始卡组 / 血量提升% / 人数上限 / 共享金币） |
| `Core/Config/TogetherSettingsStore.cs` | RitsuLib 数据存储读写 + 按种子登记共生体局（读档找回成员） |
| `Core/Config/TogetherSettingsSync.cs` | 设置的主机→客户端同步 + 主机/客户端事件挂点 |
| `Core/Config/TogetherModSettingsPage.cs` | 设置界面（开关 + 文本框） |
| `Core/Multiplayer/SymbiosisMembers.cs` | 成员集合（主机权威 + 客户端请求 + 广播 + 大厅服务绑定 + 客户端修订号重置） |
| `Core/Multiplayer/CharacterSelectPatches.cs` | 选人界面按钮注入/刷新/点击 + 起程门控 |
| `Core/Multiplayer/TogetherCoopGate.cs` | 选人阶段判定规则（名额、能否确定、能否起程） |
| `Core/Multiplayer/RunStartPatches.cs` | 新局 / 读档两个工厂的 Postfix → `Arm` |
| `Core/Multiplayer/TogetherPair.cs` | 配对注册表 + 激活流程（卡组合并、血量提升、金币、进阶之灾去重） |
| `Core/Multiplayer/SharedPilePatches.cs` | 四个战斗牌堆 + 主卡组重定向、战斗状态建立时的收尾 |
| `Core/Multiplayer/SharedPileOwnerLateNormalizePatch.cs` | 落进共享堆的牌归属归一（搬完 + 动画播完之后） |
| `Core/Multiplayer/HandOwnershipInvariantPatch.cs` | "进手牌就归手牌主人"不变量 + 记 `CardLastHandOwner` |
| `Core/Multiplayer/CardLastHandOwnerPatch.cs` | 记"最后在谁的手牌里" + 消耗钩子期间临时还原归属（金纸一类遗物） |
| `Core/Multiplayer/HandReturnOwnershipPatch.cs` | "从共享堆回手"类效果的归属与目标手牌修正 |
| `Core/Multiplayer/DiscardDrawTargetFix.cs` | "弃掉整手再抽"（计算下注等）的抽牌对象修正 |
| `Core/Multiplayer/CardOwnershipPatches.cs` | 归属规则实现（`NormalizeForHand` / `HandOwnerOf`） |
| `Core/Multiplayer/CardPileLookupPatch.cs` | `CardModel.Pile` 兜底查找（借住在另一半堆里时） |
| `Core/Multiplayer/DifferentOwnersCheckPatch.cs` | transpiler：打掉本体"同批 owner 必须一致"校验 |
| `Core/Multiplayer/DeterministicCardOrder.cs` | 洗牌前的确定性排序键（序列化等价键） |
| `Core/Multiplayer/DeterministicInitialShufflePatch.cs` | 初始洗牌前排序 |
| `Core/Multiplayer/DeterministicCardComparePatch.cs` | 让 `CardModel.CompareTo` 成为全序（战斗中洗牌 / 随机取牌） |
| `Core/Multiplayer/BodyMirror.cs` | 血量 / 最大生命 / 格挡镜像 |
| `Core/Multiplayer/PowerMirror.cs` | 能力镜像 + 镜像副本标记 + 同效果第二次命中拦截 |
| `Core/Multiplayer/MirroredTemporaryPowerPatch.cs` | 逐类跳过"回合族会改数值"的镜像副本（临时力量/敏捷/集中、虚弱/易伤/脆弱） |
| `Core/Multiplayer/HookListenerDedupePatch.cs` | 钩子监听表按**引用**去重 |
| `Core/Multiplayer/ImbuedOncePerCombatPatch.cs` | 注能（`Imbued`）每场战斗只自动打出一次 |
| `Core/Multiplayer/ExtraTurnPolicy.cs` | 额外回合不清共享格挡 |
| `Core/Multiplayer/AscensionBaneDedupePatch.cs` | 进阶之灾去重（含读档路径） |
| `Core/Multiplayer/GoldMirror.cs` | 金币共享（求和 / 取最大对齐 / 双向同步） |
| `Core/Multiplayer/PileCountSyncPatch.cs` | 抽/弃/消耗堆与卡组按钮的计数即时刷新 |
| `Core/Multiplayer/RoomFlowDiag.cs` | 房间流程里程碑 + 黑屏看门狗（自愈） |
| `Core/Multiplayer/SharedStateSelfCheck.cs` / `CappedLog.cs` / `DiagnosticPatches.cs` / `TransformSelectDiagPatch.cs` | 自检与诊断日志 |
| `Core/Multiplayer/CreaturePartnerExtensions.cs` | "组里其他人"的查询扩展 |

---

## 4. 各部分实现逻辑与挂点

### 4.1 设置与联机同步（`Core/Config/*`）

**目的**：设置必须两端一致，否则会分叉。

- `RitsuLibFramework.BeginModDataRegistration` / `GetDataStore(...).Register(key, file, SaveScope.Global, defaultFactory, autoCreateIfMissing)` / `InitializeGlobal()` / `Get<T>(key)`
- 设置界面：`RitsuLibFramework.RegisterModSettings` → `AddSection` / `AddToggle` / `AddString` / `ModSettingsValueBinding<TModel,TValue>`
- sidecar：`RitsuLibSidecarConfigSyncService.RegisterTopic<TState,TDelta>` / `PublishHostState` / `TopicChanged`
- 逻辑：主机读本地设置，客户端优先读主机广播的快照；`Effective*` 是唯一对外入口；改设置立刻广播。
- 挂点：`NetHostGameService.StartENetHost` / `StartSteamHost`（Prefix）、`SetPeerReadyForBroadcasting`（Postfix）、`NetClientGameService.Initialize`（Prefix）、`OnDisconnectedFromHost`（Postfix）。

### 4.2 选人与配对

**按钮**（`CharacterSelectPatches.cs`）
- 挂点：`NCharacterSelectScreen.OnSubmenuOpened` / `OnSubmenuClosed` / `SelectCharacter` / `PlayerChanged`（全部 Postfix）。
- `Install`：`new Button{ Name="TogetherSymbiosisButton", ... }` + `screen.AddChild` + `SetAnchorsPreset(BottomRight)` + 固定偏移钉在右下角 + `Pressed += OnPressed`；
  另挂一个 0.2s 的 `Godot.Timer` 定时刷新（避免在网络回调里碰 UI 节点）。
- 文案：`确定参加共生体（n/k）` / `共生体：已确定（n/k）· 再按一次取消` / `共生体：名额已满（n/k）`（禁用）。
- 点击：`lobby.LocalPlayer.id` → `TogetherCoopGate.CanConfirm` 本地预检 → `SymbiosisMembers.TrySet(lobby.NetService, id, confirm, "lobby_button")`。

**成员集合**（`SymbiosisMembers.cs`）
- `RegisterTopic<State, Delta>(Topic, new State(members), CanClientRequest, ApplyDelta)`：
  - `CanClientRequest(sender, delta)`：只允许 `delta.Player == sender`，且确定时 `count < Capacity`（**主机侧强制**"第三个人确定不了"）。
  - `ApplyDelta(state, delta)`：集合增删。
- 客户端：`TryRequestClientChange(...)`；主机 / 本地多控：本地判定 → `ApplyLocal` → `RegisterTopicFromLocal` → `PublishHostState`。
- 主机开新服（`StartENetHost` / `StartSteamHost`）时 `Reset`（清掉上一局成员）。
- **`BindHostService(lobby.NetService)`**：选人界面一打开就把大厅网络服务提前挂到 `RunManager`（反射写 `<NetService>k__BackingField`）。
  原因见 F20：RitsuLib 处理客户端请求时读的是 `RunManager.Instance.NetService`，而那一刻一局还没开始、值还没初始化。
- **`PrepareClientLobby(...)`**：客户端进大厅时把本地名单与修订号归 1（RitsuLib 会丢弃"修订号不比自己新"的快照广播，同一进程连过几局后本地修订号可能更高）。

**判定**（`TogetherCoopGate.cs`）：`Applies`（开关开且有联机大厅）→ `CountConfirmed` / `IsConfirmed` / `CanConfirm` / `CanEmbark`（只确定 1 个人时禁止起程）。

**配对**（`TogetherPair.Arm`，挂点：`RunState.CreateForNewRun` / `FromSerializable` 的 Postfix）
1. `SymbiosisMembers.Snapshot()` 与 `runState.Players` 求交集 → 必须 ≥2 人；不足时**保持现有配对、不清空**（读档检查、多角色会话重建都会构造 RunState）。
2. 读档 / 重连时若名单为空 → 按存档种子 `FindSymbioticRun` 找回（F19）。
3. `MergeStarterDeckInto`（开关开启、仅新局）：逐张 `fromDeck.RemoveInternal(card, silent: true)` → `toDeck.AddInternal(card, -1, silent: true)` → `card.GiveToAnotherPlayer(anchor)`。
   - **必须直接读 `DeckField(from)`**：此时 `_anchor/_echoes` 已赋值，回声的 `Player.Deck` getter 会被重定向到锚点，用 getter 读会拿到目标那一副（曾因此"合并空转"）。
   - 用"搬运"而不是"造新卡复制"，是为了**幂等**：读档重开时回声那副已空，再合不会翻倍。
4. `ApplyHpBonus`（仅新局）：`bonus = round(echo.MaxHp × percent/100)` → `SetMaxHpInternal / SetCurrentHpInternal` → `BodyMirror.SyncAll()`。
5. `DeckField(echo) = anchor.Deck`；`RunPileCache(echo) = null`。
6. `AscensionBaneDedupePatch.DedupeSharedDeck(anchor)`；`GoldMirror.OnArm(isNewRun)`。

### 4.3 共享牌库（`SharedPilePatches.cs`）

| 目标 | 类型 | 作用 |
|---|---|---|
| `PlayerCombatState.get_DrawPile` / `get_DiscardPile` / `get_ExhaustPile` / `get_PlayPile` | Postfix | 回声的 getter 返回锚点那四个实例 |
| `Player.get_Deck` | Postfix | 回声的主卡组指向锚点那一份 |
| `Player.PopulateCombatState` | Prefix（返回 false） | 只让锚点填充战斗牌堆 |
| `PlayerCombatState..ctor(Player)` | Postfix | `OnCombatStateCreated`：adopt + 清缓存 + `BodyMirror.SyncAll()` |

**为什么既要重定向 getter、又要换 backing field**：界面在初始化时会把 `PileType.X.GetPile(player)` 的引用**缓存**进自己的字段，只重定向 getter 对那份旧引用无效 → 必须替换
`<Deck>k__BackingField`、`<DrawPile>`、`<DiscardPile>`、`<ExhaustPile>`、`<PlayPile>`（`AccessTools.FieldRefAccess`）；随后把 `Player._runPiles` / `PlayerCombatState._piles` 置 `null`。
**Hand 故意不重定向**：手牌是各自独立的资源（每人 10 张上限）。

### 4.4 归属（owner）规则（三层）

一句话：**在手牌里属于手牌主人；落进共享堆后归还锚点；"最后在谁手里"另记一份供钩子还原。**

| 场景 | 实现 | 挂点 |
|---|---|---|
| 抽牌 / 把牌放进手牌（单张 + 明确堆） | `CardOwnershipImpl.NormalizeForHand`：先 `card.RemoveFromCurrentPile(true)` 再 `GiveToAnotherPlayer(handOwner)` | `CardPileCmd.Add(CardModel, CardPile, ...)` Prefix |
| 牌进手牌（数据层不变量） | `HandOwnershipInvariantPatch`：原地改 owner；**同时记 `CardLastHandOwner.Remember`** | `CardPile.AddInternal` Postfix |
| 落进共享堆（Draw / Discard / Exhaust 且确实是锚点那口堆） | `SharedPileOwnerLateNormalizePatch`：`card.GiveToAnotherPlayer(anchor)` | `Hook.AfterCardChangedPiles` Prefix（搬完 + 动画播完之后） |
| 借住在另一半堆里时反查堆 | `CardPileLookupPatch`：原查找失败再遍历 `partner.Piles` | `CardModel.get_Pile` Postfix |
| 消耗钩子期间让遗物认出"这是我的牌" | `CardLastHandOwner` 记录 + 临时还原归属，钩子跑完（含 await）再改回 | `Hook.AfterCardExhausted` Prefix/Postfix |
| 洗牌"同批 owner 必须一致"校验 | `DifferentOwnersCheckPatch`：定位 `CardPileCmd+<Add>d__N.MoveNext`，把 `newobj + throw` 换成 `pop + nop` | `[HarmonyTargetMethod]` + `[HarmonyTranspiler]` |

**为什么"进手牌要早、离手牌要晚"**：本体搬运动画期间界面会反复读 `card.Owner` / `card.Pile`（`NPlayerHand`、`CardPileCmd.MoveCardNodeToNewPileBeforeTween`）。
owner 与实际所在手牌脱钩 → 界面漏掉这张牌 → **幽灵卡**。所以离开手牌的归属改写一律放到"搬完 + 动画播完之后"。

**为什么不能"离开手牌也不归一"**：共享堆属于锚点，牌若带着回声的归属躺在锚点的堆里，`CardModel.Pile`（用 `_owner.Piles` 反查）会找不到自己的堆 → 各种 null 与静默失败。
所以"统一归锚点"是硬约束，代价是丢失"这张牌原本是谁的"这个信息 —— 用 `CardLastHandOwner` 单独补回来。

### 4.5 跨端确定性（洗牌与比较器）

**为什么需要**：本体把洗牌写成**依赖输入顺序**的算法，而共生体下两端输入顺序天然不同 →
洗出的顺序不同 → "从抽牌堆顶/随机取一张"的效果两端取到不同的牌 → 校验和不匹配 → 客户端被踢。

三处输入顺序差异：

1. 主卡组顺序（两端各自领奖励、各自加牌，先后不同）；
2. `CardPileCmd.Shuffle` 里 `list.AddRange(drawPileCards)` 枚举的是 `HashSet<CardModel>`（引用哈希 → 两端顺序必然不同）；
3. `StableShuffle` 内部虽然先 `list.Sort()`，但 `List.Sort` **不稳定**，而大量同名同升级的牌 `CompareTo` 返回 0 → 输入顺序仍会漏出来。

**做法**

| 场景 | 实现 |
|---|---|
| 初始洗牌（`CardPile.RandomizeOrderInternal` 里的 `UnstableShuffle`） | `DeterministicInitialShufflePatch`：Prefix 里按序列化等价键排序整个牌堆 |
| 战斗中洗牌（`CardPileCmd.Shuffle` 里的 `StableShuffle`）与所有"随机取牌"的卡 | `DeterministicCardComparePatch`：让 `CardModel.CompareTo` 成为**全序**（原本相等时继续按序列化键比），使 `Sort()` 的结果只取决于集合内容 |

**排序键**（`DeterministicCardOrder.SortKey`）：`JsonSerializer.Serialize(card.ToSerializable())`，失败时退化成
`楼层 | 模型 | 升级`。用序列化 JSON 是因为**校验和比对的正是这套字段** —— 键相同的两张牌互换顺序不会影响校验和。

**不要做的事（血的教训）**：**不要 patch `ListExtensions.UnstableShuffle<T>` / `StableShuffle<T>` 这类泛型洗牌方法**。
.NET 对引用类型实参的泛型方法只生成一份代码，补丁会连带命中 `UnstableShuffle<RelicModel>`（遗物抓包）、`StableShuffle<MapPointType>`（地图生成），
把非卡牌列表当卡牌列表排序 → `EntryPointNotFoundException` → **开局直接崩**（见 F22、F23）。

### 4.6 身体与能力

**身体**（`BodyMirror.cs`）
- 挂点：`Creature.Block` / `CurrentHp` / `MaxHp` 的 setter（Postfix）→ 值变了就推给另一半。
- `SyncAll()`：整份推（战斗状态建立、读档对齐、血量提升后）。
- `RepairImpossibleLifeState`：出现"一死一活"这种不可能态时按共享池语义拉平。

**能力**（`PowerMirror.cs`）
- 挂点：`Creature.ApplyPowerInternal`（Postfix → 克隆到另一半）、`RemovePowerInternal`（Postfix → 同步删除）、`InvokePowerModified`（Postfix → 同步数值）。
- **镜像副本标记**：克隆体记进 `ConditionalWeakTable<PowerModel, object>`。为什么要标记而不是"查另一半有没有对应副本"：回合末第一个结算的副本会把自己和镜像一起删掉，等派发轮到第二个副本时"另一半"已经空了。
- **`SkipNextDurationTick` 复制**：本体给玩家侧上减益时会设这个标记，克隆体是在那行之前造的，必须自己补上。
- **同一效果打到另一半 → 整个忽略**：`RecordApplication` 记（类型 + 施加者 + 层数 + 哪一半 + 时间戳），紧接着的 `PowerCmd.ModifyAmount` / `Apply` 命中"另一半 + 同类型 + 同施加者 + 同层数 + 5 秒内"就跳过（典型是 AoE 减益）。

**回合族只算一次**（`HookListenerDedupePatch.cs` + `MirroredTemporaryPowerPatch.cs`）
- `CombatState.IterateHookListeners`（Postfix）：按**引用**去重（共享牌堆里的牌会出现在两个人的 `AllPiles` 里）。
  **注意**：只去重、**不要**在这里统一过滤镜像副本 —— 会连带丢掉"每回合重置的内部计数"（实测杂耍计数整局不重置）。
  会改身体数值的少数能力改成**逐类**在 `MirroredTemporaryPowerPatch` 里跳过镜像副本。

**额外回合**（`ExtraTurnPolicy.cs`）：`Creature.ClearBlock` Prefix —— 队友没参与本回合时不清共享格挡。

### 4.7 卡牌效果类补丁

| 文件 | 挂点 | 解决什么 |
|---|---|---|
| `HandReturnOwnershipPatch.cs` | `CardPileCmd.Add(CardModel, PileType, ...)` / `(IEnumerable, PileType, ...)` / `(IEnumerable, CardPile, ...)`、`CardSelectCmd.FromCombatPile(...)` | "从共享堆拿牌回手"的目标手牌：本体用 `cards.First().Owner` 推目标手牌，共享堆里 owner 是锚点 → 回声打捏奥之怒/挖掘会进锚点手牌。改成"当前正在结算效果的那名玩家"（`CombatManager.IsExecutingCardOrPotionEffect`），认不出时退回"刚才是谁从哪个堆选的牌" |
| `DiscardDrawTargetFix.cs` | `CardCmd.DiscardAndDraw`（Prefix 记录）、`CardPileCmd.Draw(PlayerChoiceContext, decimal, Player, bool)`（Prefix 改写参数） | 计算下注 / 赌徒之酿 / 赌徒筹码：本体先弃牌、再用 `discardCards[0].Owner` 决定谁抽牌 → 回声打等于"弃自己的手牌、锚点抽牌"。改成"抽牌对象 = 那批牌原本所在手牌的主人"。**匹配时必须连抽牌张数一起比对**，否则残留记录会把别人回合的抽牌改道 |
| `ImbuedOncePerCombatPatch.cs` | `Imbued.AfterAutoPrePlayPhaseEntered` Prefix | 注能附魔牌"每场战斗开始时自动打出一次"：本体判据是 `player == Card.Owner && player.PlayerCombatState.TurnNumber <= 1`，而 `TurnNumber` 是**逐玩家**的 —— P1 结束回合后 P2 才开始它的第 1 回合，此刻 P1 的回合数仍是 1，于是同一张牌被自动打出两次。改成按"战斗 + 卡牌引用"去重 |
| `AscensionBaneDedupePatch.cs` | `AscensionManager.ApplyEffectsTo` Postfix + `Arm` | 共享卡组下"逐玩家各加一张进阶之灾"会变成两张；摘牌要连 `RunState.RemoveCard` 一起清。读档路径不经过 `ApplyEffectsTo`，所以 `Arm` 里也要对齐 |

### 4.8 UI 同步（`PileCountSyncPatch.cs`）

- 挂点：`CardPile.AddInternal` / `RemoveInternal`（Postfix → 刷新计数）、`NCombatCardPile.Initialize` / `_ExitTree`、`NTopBarDeckButton.Initialize` / `_Notification`。
- 逻辑：登记"哪个 UI 节点在看哪口牌堆"，牌堆内容一变就**直接按 `pile.Cards.Count` 写真值**（不依赖本体的 ±1 累加事件）。
- 原因：本体计数靠 `CardAddFinished/CardRemoveFinished`，而 ① 非本地玩家的牌不播动画（`GetTweenForCardsChangingPiles` 里 `LocalContext.IsMe` 直接 continue）② 静默搬运不发事件。
- 字段缓存键必须是 `(类型, 字段名)`：同一类型上要读 `_pile` / `_countLabel` / `_currentCount`，只用类型当键会把张数写进 `_pile`。
- 附带：消耗堆按钮一开始是隐藏的、靠"牌落进堆"事件显形，回声那侧等不到 → 同步时补一次显形。

### 4.9 诊断与自愈

| 组件 | 作用 |
|---|---|
| `SharedStateSelfCheck.cs` | `ChecksumTracker.GenerateChecksum(string, GameAction)` Postfix，打两个 creature 的血/格挡/状态与四个牌堆数量（默认关闭，`TOGETHER_SELFCHECK=1` 打开） |
| `CappedLog.cs` | 默认打印、每键有限条数的关键证据日志 |
| `RoomFlowDiag.cs` | 房间流程里程碑（点击节点 / 建房间 / 淡出淡入 / 取奖励 / 退出房间 / 奖励集完成） |
| **黑屏看门狗**（`RoomFlowDiag.cs` 里的 `HangWatchdog`） | 每秒检查 `NTransition` 的遮罩透明度：黑着超过 4.5 秒先跑一次本体淡入，再不行直接把遮罩透明度置 0、清 shader threshold、解除鼠标拦截、把 `InTransition` 写回 false；卡住时还会扫场景树打印"谁在挡画面" |
| `TransformSelectDiagPatch.cs` | 变牌 / 卡组选牌 / 强化 / 附魔四个入口打印"候选几张、`GetPile` 与 `player.Deck` 是否同一口堆、本机是谁" |

**看门狗的两个必要细节**（都是踩出来的）：
1. **Timer 挂在场景根节点上**，而读档 / ESC 重启房间会重建 UI 树 → Timer 被带走。早期用 `static bool _running` 记"挂过了"，于是**静默失效**；现在每次都校验节点还在不在树上，不在就重挂。
2. **不能只看 `InTransition`**：本体的 `RoomFadeIn` 有分支会先 `InTransition = false` 却把遮罩留成不透明（`showTransition=true` 且材质不是 `ShaderMaterial`）→ 老版本永远救不回来。

---

## 5. 已知问题与解法清单

### 5.1 已修复

| # | 现象 | 根因 | 解法 |
|---|---|---|---|
| F1 | 开新局 / 读档抛 `Inventory is already populated` | 引导期就开了重定向，而玩家是逐个创建的 | 分两期：引导期不重定向，run 工厂返回后再 `Arm()` |
| F2 | 双人开局**黑屏** | 激活时机过早：`RunState.CreateShared` 先设 RunState、再遍历牌组给每张卡设 owner，懒激活会让第二个玩家读到被重定向的卡组 → 同一张牌设两次 owner | 只在 `RunState.CreateForNewRun` / `FromSerializable` 的 **Postfix** 里激活 |
| F3 | 功能整体静默失效（`Harmony patched 0 method(s)`） | 方法级 `[HarmonyPatch]` 在这个 Harmony 构建下不被识别且不报错 | 改用**类级** `[HarmonyPatch]` |
| F4 | 补丁"半套生效"（卡组共享没了、能力共享还在） | `PatchAll` **非事务性**：某个补丁类抛异常会中断后续安装 | 逐类安装（`CreateClassProcessor`），统计 `applied/failed` 并写日志 |
| F5 | 角色名/卡名显示成 `TOGETHER_...` 原始键 | 本地化 JSON 活在 Godot 工程里，游戏只能通过 PCK 看到 | 导出 PCK + 清单 `has_pck: true` |
| F6 | 单人局能选中共享角色并开局 | 早期那条 UI 补丁根本没装上 | 加**权威门控**：`StartRunLobby.ChangeCharacter` 直接拒绝非法选择 |
| F7 | 战斗胜利不给卡 / 进房间卡死 | 奖励与商店要求卡池"每种类型 × 每种稀有度"都非空 | 补齐卡池（3 类型 × 3 稀有度） |
| F8 | 卡组界面 p1 显示 11 张、p2 只有 9 张 | 界面**缓存 `CardPile` 引用**，只改 getter 救不了旧引用 | 直接替换 `<Deck>` 与四个战斗牌堆的 **backing field**（`Hand` 除外） |
| F9 | 回合结束不了、战斗卡住 | 洗牌是"弃牌堆混进抽牌堆"的**批量 `Add`**，共享堆里 owner 混合 → 撞本体"同批 owner 必须一致"校验 → 异常打死回合循环 | transpiler 打掉那条校验（`DifferentOwnersCheckPatch`） |
| F10 | 变牌失败（`Couldn't get hand node` / `different owner`） | 与 F9 同源：`CardCmd.Transform` 内部也要走 `Add` 搬牌 | 随 F9 消失 |
| F11 | 变牌/改 owner 类补丁装上但永远不生效 | `CardPileCmd.Add` 是 async，transpiler 默认打在**存根**上，真实代码在编译器生成的 `MoveNext` 里 | transpiler 目标改成状态机的 `MoveNext` |
| F12 | 补丁失败日志看不出原因 | 只打了外层 `HarmonyException` | `Main.ApplyPatches` 摊平异常链输出 |
| F13 | 参数被静默注入 `null`（门控"安静地失效"） | Harmony **按名字**绑定参数，名字对不上不报错 | 关键补丁改**位置参数** `__0/__1/__2` |
| F14 | **幽灵卡**：手牌残留已打出的牌节点，点它还能操作真牌 | 归属改写时机错：在搬运动画之前就把 owner 改走 → 界面按 `card.Owner` 找手牌时漏掉 | 拆成"进手牌早（`CardPile.AddInternal`）、离手牌晚（`Hook.AfterCardChangedPiles`）" |
| F15 | p2 打「计算下注」却弃 p1 的手牌、p1 抽牌 | 本体用 `discardCards[0].Owner` 决定谁抽牌，而共享堆里 owner 已归一 | `DiscardDrawTargetFix`（记录原手牌主人） + 匹配时连抽牌张数一起比 |
| F16 | 回声打「捏奥之怒」等回手牌效果，牌进了锚点手牌 | 本体用 `cards.First().Owner` 推目标手牌 | `HandReturnOwnershipPatch`：以"正在结算效果的人"为准 |
| F17 | **注能**附魔牌回合开始自动打出**两次**（第一代） | 共享牌堆让同一张牌（连同它的附魔）被收集进监听表两次 | `HookListenerDedupePatch` 按引用去重 |
| F18 | 临时力量/敏捷回合结束多掉、减益多掉一层、中毒掉两次血 | 能力镜像是"两份都在监听表里"，回合族钩子各结算一次 | `MirroredTemporaryPowerPatch` **逐类**跳过镜像副本；`SkipNextDurationTick` 复制。**不要**用通用过滤（会连累杂耍计数不重置） |
| F19 | 读档 / 重连后共生体"散伙"（血量、卡组不再共享） | 读档时选人界面没出现过，成员名单是空的 | 新局按种子登记成员（`RememberSymbioticRun`），读档按种子找回（`FindSymbioticRun`） |
| F20 | **联机时非主机按右下角「确定参加共生体」没反应** | 成员同步走 RitsuLib 的主机权威配置同步，而主机侧那份处理读的是 `RunManager.Instance.NetService` —— 选人界面阶段一局还没开始，该属性尚未初始化 → 主机**静默丢弃**客户端请求（一条日志都没有） | `SymbiosisMembers.BindHostService(lobby.NetService)`：选人界面一打开就把大厅服务提前挂到 RunManager（与进局时本体赋值的是同一个对象） |
| F21 | 同一客户端连过几局后，选人界面一直显示"未确定" | RitsuLib 会丢弃"修订号不比自己新"的快照广播，而本地修订号可能被上一局抬高 | `PrepareClientLobby`：进大厅时把本地名单与修订号归 1 |
| F22 | **开局直接崩**（`EntryPointNotFoundException`，栈里是 `RelicGrabBag.Populate`） | patch 了泛型洗牌方法 `UnstableShuffle<CardModel>`，而 .NET 泛型共享让 `UnstableShuffle<RelicModel>` 也跑进同一个补丁；Prefix 按 `List<CardModel>` 处理遗物列表 → 绑定失败 | **不要 patch 泛型洗牌方法** |
| F23 | **开局直接崩（第二次，`StandardActMap`）** | 同上，这次是 `StableShuffle<MapPointType>`（地图生成） | 改从"比较器"一侧解决（`DeterministicCardComparePatch`），彻底不碰泛型方法 |
| F24 | 两端抽牌堆**顺序不同**导致分叉（两端各从卡组加牌，先后不同 → 初始洗牌输入不同） | 初始洗牌 `UnstableShuffle` 依赖输入顺序 | `DeterministicInitialShufflePatch`：初始洗牌前按序列化键排序 |
| F25 | 打出**破灭**（`HAVOC`）等"从抽牌堆随机取牌"的牌后立刻分叉、客户端被踢 | `StableShuffle` 的输入顺序两端不同（HashSet 枚举顺序 + 不稳定排序）→ 两端取到不同的牌 | `DeterministicCardComparePatch`：让 `CardModel.CompareTo` 成为全序，使 `Sort()` 只取决于集合内容 |
| F26 | **P2 的金纸（`JossPaper`）不响应自己消耗的牌** | 金纸判据是 `card.Owner == base.Owner`，而牌落进共享消耗堆时已被归一成锚点 | `CardLastHandOwner` 记录"最后在谁手里" + `Hook.AfterCardExhausted` 派发期间临时还原归属（含 await 之后恢复） |
| F27 | 转场黑屏（游戏活着但画面全黑） | 本体的 `RoomFadeIn` 在某些分支（材质不是 `ShaderMaterial`）会先 `InTransition = false` 却把遮罩留成不透明；另外看门狗 Timer 会被 UI 树重建带走 | `HangWatchdog`：Timer 生命周期自检 + 改看遮罩透明度 + 两级救援（先跑本体淡入，再兜底硬复位） |
| F28 | 回合结束不清共享格挡 / 额外回合清掉队友格挡 | 额外回合的 `ClearBlock` 与共享格挡语义冲突 | `ExtraTurnPolicy`：队友没参与本回合时不清 |
| F29 | 进阶之灾在共享卡组里变成两张 | 本体逐玩家各加一张 | `AscensionBaneDedupePatch`（含读档路径对齐） |
| F30 | 金币不共享 / 商店买卡不同步 | 金币是逐玩家的 | `GoldMirror`：`Player.Gold` setter + `PlayerCmd.LoseGold` 双挂点；新局求和（99×n），读档取最大值对齐 |

### 5.2 未解决 / 待办

| # | 问题 | 现状与下一步 |
|---|---|---|
| U1 | **"从卡组选牌"的界面在某端不弹**（附魔 / 先古克隆 / 变牌 / 休息处强化） | 已加诊断 `TransformSelectDiagPatch`（打印候选张数、`GetPile` 与 `player.Deck` 是否同一口堆、本机是谁）。观测到的现象：同一个"强化"选项，回声正常弹窗并选中，锚点却返回 `PlayerChoiceResult deck`（空）且 `success False`。需要在真实联机复现并拿诊断行定位是"候选为空"还是"界面开给了别人" |
| U2 | 卡牌**费用覆盖**两端不一致（分叉） | 诊断包显示：同一张 `VICIOUS +1` / `JUGGERNAUT`，客户端带 `cost=2` / `cost=3`，主机是原价，且发生在战斗刚开始（checksum #0）。指向"附魔 / 减费类效果只在一端落地"，与 U1 同源 |
| U3 | 抽/弃牌堆计数 UI 不即时刷新 | 纯 UI（控件缓存牌堆引用、只在切换控制权时重建）。数据层已正确，暂缓 |
| U4 | `All rewards have been taken, but the rewards set is not complete on the backend!` | 本体抱怨：拿到的 `RewardsSet.Id` 仍是 -1（从未被 `BeginRewardsSet` 登记）。发生在退出房间、清空奖励按钮之后，不阻断转场，暂缓 |
| U5 | 随机数预测 mod（RandomForeseer）在共享堆上报 `Cannot add cards with different owners to the same pile` | 它自己的预测代码撞上本体的归属校验，被它自己 catch；只影响它的预测显示 |
| U6 | 3~4 人局 | 代码按"组"写（镜像 / AoE 去重 / 卡组合并 / 血量上限都是按成员集合算），只在 2 人局验证过 |
| U7 | 真实双机联机 | sidecar 同步链路在真实联机下已部分验证（成员同步、读档找回、金币都对上了），但覆盖面仍不足 |

### 5.3 流程性教训

| # | 问题 | 对策 |
|---|---|---|
| P1 | 修复文件被**整目录覆盖 / 误删**（`.cs` 消失、只剩 `.uid`） | 只拷贝**本次改动**的文件；部署后报清单；`.uid` 残留而 `.cs` 缺失 = 文件被删的信号 |
| P2 | 补丁失败静默或难查 | 逐类安装 + `applied/failed` 计数 + 异常链日志 |
| P3 | **在本地多控上验证跨端问题** | 本地多控是**单进程回环**：两个玩家共享同一批 `CardModel` 与同一个 `RunState`，校验和不会报分叉 —— "两端不一致"类问题**测不出来**。而且它的 `CardSelectCmdPatch` 会把 `ShouldSelectLocalCard` 强制改成"本地手选"，**掩盖**选牌界面问题。跨端问题必须双机验证 |
| P4 | 交给 Harmony 的泛型方法 | 见 F22/F23：**不要 patch 泛型方法**（尤其洗牌/集合扩展） |
| P5 | 拿不准就先加证据 | 每次分叉都由 RitsuLib 生成诊断包（`ritsulib_state_divergence_*.zip`），里面有两端逐字段对比，可直接定位差异项 |

---

## 6. 可能的问题（还没踩到，但按当前设计迟早会遇到）

### 6.1 归属与"认领我的牌"

- **`CardLastHandOwner` 目前只挂在消耗钩子上**（金纸）。同样用 `card.Owner == 自己` / `cardPlay.Card.Owner == 自己` 认领的还有：出牌（`AfterCardPlayed`）、弃牌（`AfterCardDiscarded`）、抽牌（`AfterCardDrawn`）、以及各种 `Hook.ShouldPlay` / 遗物触发。**下一个"某遗物不响应 P2"的报告大概率来自这些钩子**，修法相同（记录已就绪，加一处分发即可）。
- 牌**从未进过手牌**就被消耗（从抽牌堆直接打出/消耗）时没有记录 → 仍按锚点归属判断，这类效果在 P2 侧仍可能不触发。
- 镜像副本与"认领"的相互作用未系统梳理：`PowerMirror` 的副本挂在另一半身上，凡是用 `power.Owner` 判断的遗物/能力都可能认错人。

### 6.2 跨端一致性

- **"两端各自本地执行 + 互相同步"的操作都可能顺序不同**：卡牌奖励、事件选项、商店购买、消耗品。已处理的有：初始洗牌、战斗中洗牌、随机取牌；**未处理**的有：牌的费用覆盖、附魔、升级、以及任何"给牌加字段"的效果（对应 U2）。
- `HashSet` / 字典枚举顺序、`List.Sort` 的不稳定性、`RuntimeHelpers.GetHashCode` 这类"进程内确定、跨进程不同"的东西，仍然是**同类风险的来源**；新加代码时不要依赖它们。
- `DeterministicCardOrder.Sort` 与 `DeterministicCardComparePatch` 都走 `ToSerializable()`，性能开销随牌数增长（见 6.4）。

### 6.3 界面与流程

- **"该谁选牌"判定**：`CardSelectCmd.ShouldSelectLocalCard(player) => LocalContext.IsMe(player)`。共生体下如果"发起者"在某一端被解析成别人，就会出现"两端都在等对方"（卡死）或"都没弹"（静默跳过）。U1 就属于这一类，修好之前**所有需要选牌的效果都不可靠**。
- 奖励屏 / 事件 / 商店的多玩家汇总流程（多角色 mod 负责的部分）与共生体的组合没有系统验证过（U4）。
- 黑屏看门狗只能救"遮罩型"黑屏；如果是别的东西盖住画面（其它 mod 的全屏层、3D 后处理），它只能**打印出挡画面的节点路径**，救不了。

### 6.4 性能与日志

- `DeterministicCardComparePatch` 让每次排序都要序列化卡牌；卡组界面排序、洗牌、`Sort()` 密集的界面都可能感到卡顿。若出现卡顿，方向是给 `SortKey` 加缓存（按牌的"状态版本"失效）。
- 诊断日志（`CappedLog` 每键有限条数）在长局里会打满，之后就没有证据了 —— 排查期应确认目标键还没到上限，或临时调大上限。
- `RoomFlowDiag`、`TransformSelectDiagPatch` 这类补丁在生产环境是**净开销**，正式发布前可以整体删掉或降级成 `SelfCheck`。

### 6.5 兼容与边界

- `DifferentOwnersCheckPatch` 打掉了本体的归属校验（F9），**畸形输入会被放行**：曾经出现过 `Hook.AfterCardEnteredCombat` 的 `combatState` 为 null 导致 NRE。更稳的方向是把绕过**收窄到"目的堆是共享堆"**时才生效。
- 与其它 mod 的组合：任何"遍历牌堆 + 假设 owner 唯一"的第三方代码（随机数预测、手牌显示、UI 增强）都可能撞上共享牌堆，轻则刷日志、重则抛异常。
- 存档与版本升级：`Arm` 里的"开局一次性"逻辑都靠 `isNewRun` 区分；如果将来新增"读档也要做"的初始化，必须自己写幂等。
- **3~4 人局**（U6）：`Capacity` 可调到 4，但"第三个人确定不了"这类规则、镜像的广播半径、AoE 去重的匹配窗口都没在 4 人下验证过。

---

## 7. 验证清单（每次改动后跑一遍）

**部署前**
1. 只拷贝本次改动涉及的文件到 `E:\game_part2\mod_c\sts2\mod\remake\together\`（不要整目录覆盖）；
2. 确认目标文件里没有"只剩 `.uid`"的孤儿；
3. 关掉游戏（DLL 被 `SlayTheSpire2.exe` 锁住会导致拷贝失败）；
4. `dotnet build .\together.csproj -c Debug -p:RunPckExport=false`，确认 `0 警告 0 错误`，并检查日志里 `patch classes applied=xx failed=0`。

**单机 / 本地多控（快速筛逻辑错误）**
5. 开一局共生体：血量合并、卡组张数 = p1 + p2、金币求和；
6. 战斗：抽牌堆只有一份、手牌各自 10 张上限、能量各自；
7. 打一张"归属相关"的牌（计算下注 / 捏奥之怒）确认目标正确；
8. 看 `[together] 洗牌前先按确定性顺序排列 N 张牌` 是否出现。

**真实双机（跨端一致性）**
9. 双方各领一次卡牌奖励 → 进战斗，看有没有 `State divergence detected`；
10. 用**破灭 / 灾变 / 骚动**这类"从抽牌堆随机取牌"的牌打一场；
11. 让 P2 消耗自己的手牌，确认金纸计数（日志 `消耗结算：把 XXX 的归属临时还给 netId=…`）；
12. 触发一次"从卡组选牌"（附魔 / 克隆 / 强化），对照诊断行判断 U1；
13. 读档一次，确认 `读档：按种子找回共生体成员 [...]` 与本局人数；
14. 出现 `State divergence` 时立刻取走 `logs\ritsulib_state_divergence_*.zip`，解出 `state-divergence-report.txt` 比对差异项。

---

## 8. 挂点速查表

| 本体函数 / 类型 | 补丁 | 目的 |
|---|---|---|
| `RunState.CreateForNewRun` / `FromSerializable` | Postfix | 激活配对（`Arm`） |
| `Player.get_Deck`、`PlayerCombatState.get_DrawPile/DiscardPile/ExhaustPile/PlayPile` | Postfix | 回声访问重定向到锚点 |
| `Player.PopulateCombatState` | Prefix(false) | 只让锚点填充战斗牌堆 |
| `PlayerCombatState..ctor(Player)` | Postfix | adopt 锚点牌堆 + 清缓存 + 身体对齐 |
| `CardPile.AddInternal` | Postfix | 进手牌归属对齐 + 记最后持有者 + 计数刷新 |
| `Hook.AfterCardChangedPiles` | Prefix | 落进共享堆后归属归一（晚） |
| `Hook.AfterCardExhausted` | Prefix/Postfix | 消耗结算期间临时还原归属（金纸一类） |
| `CardModel.get_Pile` | Postfix | 借住另一半堆时的兜底查找 |
| `CardPileCmd+<Add>d__N.MoveNext` | Transpiler | 打掉"同批 owner 必须一致"校验 |
| `CardPile.RandomizeOrderInternal` | Prefix | 初始洗牌前排序 |
| `CardModel.CompareTo` | Postfix | 补成全序，让 `StableShuffle` 确定性 |
| `CombatState.IterateHookListeners` | Postfix | 监听表按引用去重 |
| `Imbued.AfterAutoPrePlayPhaseEntered` | Prefix | 注能每场战斗只打一次 |
| `Creature.Block/CurrentHp/MaxHp` setter | Postfix | 身体镜像 |
| `Creature.ApplyPowerInternal` / `RemovePowerInternal` / `InvokePowerModified` | Postfix | 能力镜像 |
| `Creature.ClearBlock` | Prefix | 额外回合不清共享格挡 |
| `AscensionManager.ApplyEffectsTo` | Postfix | 进阶之灾去重 |
| `Player.Gold` setter / `PlayerCmd.LoseGold` | Prefix/Postfix | 金币共享 |
| `NCharacterSelectScreen.OnSubmenuOpened/OnSubmenuClosed/SelectCharacter/PlayerChanged` | Postfix | 共生体按钮与门控 |
| `NTransition.RoomFadeOut/RoomFadeIn`、`RunManager.EnterMapCoord/EnterMapPointInternal/Launch/ExitCurrentRoom`、`NRewardsScreen.RewardCollectedFrom`、`RewardsSetSynchronizer.CompleteRewardsSet` | Prefix/Postfix | 流程里程碑与黑屏看门狗 |
| `CardSelectCmd.FromDeckGeneric/FromDeckForUpgrade/FromDeckForEnchantment/FromDeckForTransformation/FromCombatPile` | Prefix | 选牌诊断 / 回手目标修正 |
