> **本文件已并入《架构与实现说明.md》**（第 5 节"已知问题与解法清单"、第 6 节"可能的问题"）。
> 这里只作历史留存，最新结论以架构说明为准。

# together 联机实测：问题与解法清单（历史）

> 环境：本机双人（DualRoleAdventure 回环主机，`NetGameType.Host`、无真实对端）。
> 期间还用了原版角色测试模式（把 Defect 当共享角色）来验证。

---

## A. 已修复（含当时的现象与根因）

| # | 现象 | 根因 | 解法 |
|---|---|---|---|
| A1 | 开新局/读档抛 `Inventory is already populated` / `Deck has already been populated` | 引导期就开了重定向：`IsInventoryPopulated` 把"卡组非空"当已填充，而玩家是逐个创建的 | 分两期：引导期不重定向；run 工厂返回后再 `Arm()` |
| A2 | 双人开局**黑屏** | 激活时机过早：`RunState.CreateShared` 是"先设某玩家 RunState、再遍历其牌组给每张卡设 owner"，懒激活会让第二个玩家读到被重定向的卡组 → 同一张牌设两次 owner → `already has an owner` | 只在 `RunState.CreateForNewRun` / `FromSerializable` 的 **Postfix** 里激活 |
| A3 | 功能整体静默失效（`Harmony patched 0 method(s)`） | 方法级 `[HarmonyPatch]` 在当前 Harmony 构建下不被 `PatchAll` 识别，且不报错 | 改用**类级** `[HarmonyPatch]` |
| A4 | 补丁"半套生效"（卡组共享没了、能力共享还在） | `PatchAll` **非事务性**：某个补丁类抛异常会中断后续安装 | 改为逐类安装（`CreateClassProcessor`），统计 `applied/failed` 并写日志 |
| A5 | 角色名/卡名显示成 `TOGETHER_...` 原始键 | 本地化 JSON 活在 Godot 工程里，游戏只能通过 PCK 看到 | 导出 PCK + 清单 `has_pck: true` + `RunPckExport` 默认 true |
| A6 | 单人局能选中共享角色并开局 | UI 层 `IsLocked` 伪装不够（且早期那条补丁根本没装上） | 加**权威门控**：`StartRunLobby.ChangeCharacter` 里直接拒绝非法选择 |
| A7 | 战斗胜利不给卡 | 奖励生成要求 Common/Uncommon/Rare 桶都非空，卡池只有 2 张 Basic | 补卡：3 稀有度都至少一张 |
| A8 | 进房间卡死（问号/商店） | 商店生成库存要求**每种类型**都有牌，卡池没有 `CardType.Power` → 异常让 `MoveToMapCoordAction` 带异常结束 | 补 Power 卡，并把卡池补成 **3 类型 × 3 稀有度** |
| A9 | 卡组界面 p1 显示 11 张、p2 只有 9 张 | 界面（顶栏卡组按钮/卡组界面/战斗牌堆控件）会**缓存一次 `CardPile` 引用**，只改 getter 救不了旧引用 | 直接替换 `<Deck>k__BackingField` 与四个战斗牌堆的 backing field（`Hand` 除外） |
| A10 | 回合结束不了、战斗卡住 | 洗牌 = "把弃牌堆混进抽牌堆"的**批量 `Add`**，而共享堆里 owner 混合 → 撞本体"同批 owner 必须一致"校验 → 抛异常打死回合循环 | 用 **transpiler 打掉那条校验**（见 B2 的注意事项） |
| A11 | 变牌失败（`Couldn't get hand node` / `different owner`） | 与 A10 同源：`CardCmd.Transform` 内部也要走 `Add` 搬牌，被那条校验拖死 | 随 A10 一起消失 |
| A12 | 变牌/改 owner 类补丁装上但永远不生效 | `CardPileCmd.Add` 是 `async`，Harmony transpiler 默认打在**存根**上，真实代码在编译器生成的 `MoveNext` 里 | transpiler 目标改成状态机的 `MoveNext`（扫描内嵌类型中 IL 含目标字符串的那个） |
| A13 | 补丁失败日志看不出原因 | 只打了外层 `HarmonyException`，真实原因在内层异常里 | `Main.ApplyPatches` 摊平异常链输出 |
| A14 | 参数被静默注入 `null`（门控"安静地失效"） | Harmony **按名字**绑定参数，名字对不上不报错 | 关键补丁改**位置参数** `__0/__1/__2` |

**实测已通过的功能**：选人门控 / 双人配对 / 共享卡组 / 共享抽弃堆 + 各自手牌能量 / 血量格挡能力共享 / 死亡与复活 / 存读档重连 / 非战斗流程。

---

## B. 未解决（按优先级）

### B1【最高优先】幽灵卡

**现象**：手牌里残留已打出的牌的节点；点它能真的操作那张卡（但归属已不是自己）。

**根因**：为了迎合 A10 那条校验，我们把"离开手牌的牌"统一改成锚点归属 → `CardModel.Pile` 靠 owner 反查堆，owner 改了之后反查失效 → 本体"移除手牌节点"的判断不成立。

**现在可以撤了**：A10 已用 transpiler 根治（校验失效、不再需要改 owner）。

**要做两处**：

1. 删 `Core/Multiplayer/SharedPileOwnerInvariantPatch.cs`（我的文件，可自行处理）
2. 停用 `CardOwnershipPatches.cs` 里的 `NormalizeBatchIfMixed`（**用户改过的文件，需确认**）

**必须保留**：`NormalizeForHand`（进谁的手牌归谁）—— 否则回声打自己抽到的牌会扣锚点的能量。

**验证**：回合结束弃牌后手牌干净；打自己抽到的牌扣自己的能量。

### B2 原版角色联机时 `AfterCardEnteredCombat` 的 NRE（`combatState` 为 null）

**判断**：很可能是 A10"绕过校验"的**副作用** —— 一张 `CombatState` 为 null 的牌本该被那条校验挡下（`Add` 里就是 `Hook.AfterCardEnteredCombat(card.CombatState, card)`），绕过之后这类畸形输入会真的流下去。

**不要用**这个写法（它自己抵消了保护）：

```csharp
combatState ??= card.Owner?.Creature.CombatState;   // ❌ 把本该跳过的通知放行了
```

参数只决定"向谁派发"，监听者自己会读 `card.CombatState`，顶参数救不了它。

**正确的最小守卫**：

```csharp
private static bool Prefix(ICombatState? __0, ref Task __result)
{
    if (__0 is not null) return true;
    __result = Task.CompletedTask;   // 牌不在战斗状态里 → 这次通知按语义就该跳过
    return false;
}
```

**更推荐的两条路**：

1. **先不定守卫**，开 `TOGETHER_SELFCHECK=1` 复现一次，用自检日志定位"哪张牌、谁的 owner、哪个堆"把 null 带进来
2. **收窄绕过**：只在 `newPile` 是共享堆（Draw / Discard / Exhaust）时才绕过校验，其它目的地照旧走本体校验 → 既保住洗牌与变牌，又不放畸形输入过去

### B3 抽/弃牌堆计数不即时刷新

纯 UI：战斗牌堆控件缓存了牌堆引用、只在切换控制权时才重建。**数据层已验证正确**（用户决定先跳过）。

### B4 `All rewards have been taken, but the rewards set is not complete on the backend!`

奖励后端的抱怨，尚未观察到对应的可见症状（疑与多角色 mod 的汇总奖励流程有关）。

### B5 `PlayCardAction … finished execution, but was in state Canceled!`

出牌动作被暂停/取消的状态提示，实测出牌表现正常。

---

## C. 流程性问题（今天两次"修复复发"的原因）

| # | 问题 | 对策 |
|---|---|---|
| C1 | 修复文件被**整目录覆盖 / 误删**：`HandCardNodeCleanupPatch.cs` 的 `.cs` 消失（只剩 `.uid`）、归属归一化被重写掉 | 只拷贝**本次改动**的文件；每次部署前确认目标文件存在、部署后报清单 |
| C2 | 补丁失败静默或难查 | 已改进：逐类安装 + `applied/failed` 计数 + 异常链日志；`.uid` 残留而 `.cs` 缺失 = 文件被删的信号 |

---

## D. 建议的收尾顺序

1. **撤归一化**（B1）→ 一次验证四项：回合能过 / 变牌正常 / 无幽灵卡 / 打自己抽到的牌扣自己能量
2. **处理 NRE**（B2）：先开自检复现定位；必要时把绕过收窄到"仅共享堆"
3. **UI 计数刷新**（B3）：等手感受影响再修，方向是让牌堆控件重建时重新取一次共享牌堆
4. 之后才是内容与表现层：卡池扩充、角色美术、血条合一/站位/受击反馈
