# 双人共享角色设计方案（together）

> 适用范围：STS2 + .NET 9 + RitsuLib 0.5.18。本文所有引擎结论均对着
> `orign/src/`（本体反编译）与 `STS2-RitsuLib/src` 核过，关键处标注了符号名，便于落地时复查。

---

## 0. 前置：总体架构与基础事实

### 0.1 核心思路：不做「同步」，做「同一个对象」

STS2 是**确定性锁步**同步：网络上跑的是**动作**（GameAction），状态由两端各自本地推导。
格挡、生命、状态、牌堆**都不走网络**。因此：

- 不存在「同步频率太高」的问题——根本没有包要发
- 真正的成本是「同步」这个词本身：每一次状态抄写都是一个可能漏、可能顺序错、可能两端算出不同结果的地方
- 正确做法是让这些状态**只存在一份**，或让两端的推导**共用同一个对象**。
  这样一次动作 = 一次本地对象变更，两端自动一致，同步点归零

### 0.2 锚点 / 回声模型

引擎强制每个玩家各有一个 `Creature`（`CombatState.Players` 是从
`PlayerCreatures.Select(c => c.Player)` 反推出来的，少一个 creature 就少一个玩家，
能量/手牌/回合全部崩掉）。所以做法是：

| 角色 | 称呼 | 职责 |
|---|---|---|
| `runState.Players[0]` | **锚点（p1）** | 权威实例持有者：主卡组、战斗牌堆、身体真实值 |
| `runState.Players[1]` | **回声（p2）** | 访问入口重定向到锚点；自己的手牌/能量独立 |

**锚点判定只能用两端一致的数据**：`RunState.Players` 的顺序或 NetId 排序。
该顺序来自大厅（`LobbyBeginRunMessage.playersInLobby`），两端必然一致，
且会写进存档，重连后不变。

> **硬纪律**：绝对不能用 `LocalContext` 这类「本机视角」的信息决定锚点或同步方向，
> 否则两个 peer 会往相反方向锚定，第一次抽牌就分叉。

### 0.3 共享 / 分离总表（本次决定：遗物不镜像）

| 状态 | 归属 | 说明 |
|---|---|---|
| 生命 / 最大生命 / 格挡 | **共享** | 一个池。不是「双倍」，是人只有一个身体 |
| 增益 / 减益（能力） | **共享** | 默认镜像两份（等价原版双人局），例外走白名单 |
| 主卡组 | **共享** | run 期唯一的 `Player.Deck` |
| 抽牌堆 / 弃牌堆 / 消耗堆 / 打出堆 | **共享** | 一套循环 |
| 地图 / 事件 / 商店决策 | 共享 | 本体多人本来就靠投票，零改动 |
| **手牌** | 分离 | 各自 5 张，上限 10（`CardPile.MaxCardsInHand`） |
| **能量** | 分离 | 各自 3 点 |
| **星星** | 分离 | 与能量同属「行动资源」 |
| **遗物** | **分离（不镜像）** | 各拿各的，等价原版双人局 |
| 药水 | 分离 | 各 3 格 |
| 金币 | 分离（建议） | 见 §5.5，可选改成共享钱包 |

单人局不可选；3-4 人局不可选（本体 `NMultiplayerHostSubmenu` 上限 4 人）。

### 0.4 关键引擎事实速查（落地时按表复查）

| 事实 | 依据 |
|---|---|
| 多人局是**同时操作**：每人各自手牌/能量，全员按完结束回合才进敌方回合 | `CombatManager.StartTurn` / `AfterAllPlayersReadyToEndTurn` |
| 每人 `SetupPlayerTurn` 按 `Players` 顺序**逐个 await** | 同上（抽牌顺序天然确定） |
| 牌堆唯一的写入通道是 `CardPile` 的 6 个方法 | `CardPile._cards` 私有、`Cards` 只读 → `AddInternal` / `RemoveInternal` / `MoveToTopInternal` / `MoveToBottomInternal` / `Clear` / `RandomizeOrderInternal` |
| `PlayerCombatState.AllPiles` 是**首次访问即固化的数组**，构造函数里就会访问（订阅 StateTracker） | `PlayerCombatState` 构造函数 |
| 进战斗时 `ResetCombatState()` + `PopulateCombatState(rng, state)`，后者把主卡组**克隆**进抽牌堆并设 `DeckVersion` | `Player.PopulateCombatState`；调用方 `CombatManager` 与 `CombatRoom` |
| 抽牌取 `drawPile.Cards.FirstOrDefault()`（索引 0 是堆顶） | `CardPileCmd.DrawInternal` |
| 洗牌判定：**该玩家自己的**抽牌堆空 && **该玩家自己的**弃牌堆非空才洗；洗牌只读该玩家自己的弃牌堆 | `CardPileCmd.ShuffleIfNecessary` / `Shuffle` |
| `CardModel.Owner` 有主不可改（旧新都非空则抛异常） | `CardModel.Owner` setter |
| 跨界交牌的正规通道 | `CardModel.GiveToAnotherPlayer(player)` + `CardPileCmd.Add(..., isChangingOwners: true)` |
| `CardModel.Pile` 是**计算属性**，只在该卡自己的 owner 的堆里找自己 | `CardModel.Pile` |
| 战斗内克隆**保留原 owner**（`AfterCloned` 不重置 `_owner`） | `CardModel.AfterCloned` |
| 同一次 `Add` 调用里多张牌 owner 必须一致，否则抛异常 | `CardPileCmd.Add` |
| `Creature.Block` / `CurrentHp` / `MaxHp` 是 private setter + `!=` 变化判断；`_block` 只在 setter 里被写 | `Creature` |
| 回合开始时清格挡（turn 1 除外）；额外回合只让特定玩家参与 | `Creature.AfterTurnStart` / `ClearBlock` / `CombatManager.StartTurn` |
| 钩子是**全量广播**：每个能力都会收到战斗里的每个事件，自己用 owner 判断要不要管 | `Hook.*` → `IterateHookListeners`；例 `PanachePower.AfterCardPlayed` 里 `if (cardPlay.Card.Owner != Owner.Player) return;` |
| 能力的写入收口是 `ApplyInternal` / `RemoveInternal` / `SetAmount`（都 public） | `PowerModel` |
| 校验和按 creature（`currentHp/maxHp/block/powers`）+ 按玩家（`Hand/DrawPile/DiscardPile/ExhaustPile/PlayPile`）序列化后 `XxHash32` | `NetFullCombatState.FromRun` |
| 校验和不一致 → 主机踢人（`NetError.StateDivergence`） | `ChecksumTracker` |
| 存档**不含任何战斗状态**（无抽牌堆/手牌/战场） | `SerializableRun` / `SerializablePlayer` |
| 引导期守卫：卡组非空就算「已填充」 | `Player.IsInventoryPopulated` / `LoadInventory` / `PopulateDeck` |
| 玩家创建/读档是**逐个玩家**调用，早于能确定锚点的时机 | `Player.CreateForNewRun` / `FromSerializable` |
| 遗物获取的唯一收口 | `RelicCmd.Obtain(RelicModel, Player, int)` → `Player.AddRelicInternal` |
| 角色选择界面用 `IsLocked` 决定是否禁用起程 | `NCharacterSelectScreen.SelectCharacter` |
| 玩家换角色是全网同步的 | `StartRunLobby` + `LobbyPlayerChangedCharacterMessage` |
| 多人握手校验 gameplay mod 列表 + ModelId 库哈希 | `HandshakeManager`（`ModMismatch` / `VersionMismatch`） |

---

## 1. 共享牌库的实现方案

### 1.1 重定向清单

回声玩家的这几个访问入口返回锚点的**同一实例**：

```
Player.Deck                        （run 期主卡组）
PlayerCombatState.DrawPile
PlayerCombatState.DiscardPile
PlayerCombatState.ExhaustPile
PlayerCombatState.PlayPile
```

**不动 `Hand`**。手牌保持独立，于是每人各自的 10 张上限、能量、UI 全部天然成立。

实现方式用 Harmony 前缀补丁改写 getter 的返回值（这些都是 get-only 自动属性，
所有访问都经过 getter，包括 `CardPile.Get(PileType, player)` 这个公共入口），
比「运行时替换 backing field」稳——因为 `AllPiles` 是首次访问即固化的数组，
而它会在 `PlayerCombatState` 构造函数里（订阅 StateTracker）就被访问。

### 1.2 弃牌堆为什么必须一起共享

洗牌判定是「**该玩家自己的**抽牌堆空 && **该玩家自己的**弃牌堆非空」，
且洗牌只读该玩家自己的弃牌堆。

如果保留各自独立的弃牌堆：p2 的抽牌堆（共享）抽空了、但他自己的弃牌堆可能是空的
→ 洗牌不触发 → **这场战斗他再也抽不到牌（软锁，不崩）**。

所以四堆要么全共享，要么把 `ShuffleIfNecessary` / `Shuffle` 一起接管。
**推荐全共享**：洗牌逻辑天然正确、零代码。

### 1.3 引导期与 Arm 时机（最容易炸的一环）

`Player` 的创建/读档路径里有三道守卫：

```csharp
private bool IsInventoryPopulated =>
    (!Deck.Cards.Any() && !Relics.Any()) ? Potions.Any() : true;   // 卡组非空即算"已填充"

LoadInventory(save):
    if (IsInventoryPopulated) throw new InvalidOperationException("Inventory is already populated.");
    PopulateDeck(save.Deck...);

PopulateDeck(cards):
    if (Deck.Cards.Any()) throw new InvalidOperationException("Deck has already been populated.");
```

而玩家是**逐个** created / loaded 的，**早于**能确定 p1/p2 的时机。
所以一旦重定向从头到尾生效：p1 填完卡组后，p2 的填充立刻抛异常——**开新局/读档必炸**。

方案：分两期。

1. **引导期（创建 / 读档）不上锁**：两人各自按本体流程填充自己的卡组，
   得到两份内容相同的副本，所有守卫正常通过
2. **run 建成后 `Arm()`**：确定锚点、开启重定向，回声那份副本变成孤儿
   （清掉或留着都行，只要两端一致）

`Arm()` 建议挂在一个明确的 run 生命周期点上（RitsuLib 的 run 开始事件），
不要挂在「第一次战斗开始」——中间夹着的商店/事件也会写卡组。

### 1.4 进战斗

| 方法 | 处理 |
|---|---|
| `Player.ResetCombatState()` | **两个人都跑**（回声需要 `PlayerCombatState` 来放手牌和能量） |
| `Player.PopulateCombatState(rng, state)` | **只跑锚点一次** |

第二条是硬要求：它干的事是「遍历 `Deck.Cards`，每张 `CloneCard` 一份塞进 `DrawPile`，
然后洗牌」。回声也跑的话，同一套主卡组会被克隆两遍倒进同一个共享抽牌堆
——直接变成**双倍卡组**。两边用的是同一个 `RunState.Rng.Shuffle`，只跑一次就两端一致。

补丁打在方法本体上（而非调用点），这样 `CombatManager` 与 `CombatRoom` 两处调用一起覆盖。

### 1.5 战斗中天然成立的几件事

因为四堆是同一个对象，下面这些**不需要任何额外代码**：

- p2 抽牌 → 从共享堆取顶（索引 0），加进 p2 自己的 `Hand`
- p2 弃牌 / 消耗 / 打出 → 进共享的弃牌堆 / 消耗堆 / 打出堆
- 洗牌 → 判定条件对两人是同一份状态，**天然正确**
- 手牌上限、能量、UI → 各自独立
- **抽牌顺序**：`StartTurn` 里 `SetupPlayerTurn` 按 `Players` 顺序逐个 await，
  所以「p1 抽完 5 张才轮到 p2 抽 5 张」是本体给的；中盘两人同时打抽牌效果时
  由动作队列串行化。两端执行同一串动作，结果必然一致

### 1.6 存档

- 存档里**没有战斗状态**（`SerializableRun` / `SerializablePlayer` 只存 deck/relics/hp/gold/rng 等）。
  所以读档后共享抽牌堆是由 `PopulateCombatState` 重建的 → §1.4 的「只跑一次」同样适用
- **回声的主卡组序列化成空**。`SerializablePlayer.Deck` 带
  `[JsonSerializeCondition(SaveIfNotCollectionEmptyOrNull)]`，空列表会被直接省略，于是：
  存档里只有一份卡组（体积不翻倍）、读档时回声的 `PopulateDeck(空列表)` 天然通过守卫、
  也不会撞上「already has an owner」
- 血量/格挡在 `Arm()`（含每次读档后）用锚点的值**强制覆盖**回声，不信存档里的回声数据
  （否则一次镜像 bug 会被固化，从此每次都从错误状态起步）

### 1.7 一致性由谁保证

不是靠同步消息，而是靠**规则本身是确定性函数**：

- 一切判断只依赖 `RunState.Players` 顺序、NetId 这类两端一致的数据
- 不用 `LocalContext`、不用本地时钟、不用「字典遍历顺序」决定行为
- 所有变更由本体动作队列驱动（本来成立，战斗动作全是 GameAction）
- 镜像逻辑**零 RNG 消费**

`ChecksumTracker` 会持续比对两端。共享实例两端一致、变更由同一串动作驱动，
哈希天然一致——**它一旦报分叉，就说明上面某条纪律被破了**，是很好的自检信号。

---

## 2. 卡牌奖励选择

### 2.1 默认：保持「各选各的」（等价原版双人局）

本体奖励流程是**每个玩家各有一份**（`RewardSynchronizer` / `RewardsSetSynchronizer`），
两人各自看到自己的奖励集合并各自选。共享卡组下，这意味着**一次战斗会有两张卡进同一个卡组**。

这不是 bug，而是**平衡上正确的**：怪物血量按玩家数 ×1.1~1.3 缩放
（`Creature.ScaleMonsterHpForMultiplayer` / `MultiplayerScalingModel`），
敌人是按「两个完整角色」配的；我方输出端也确实是一个角色体系下的两双手
（6 能量、每轮 10 张牌）。所以**卡组也要按两个人的速度成长**，否则打不过。
若只给一份奖励，卡组会以单人速度膨胀而敌人按双人缩放，必然越来越吃力。

所有「往卡组加牌」的路径都落在同一份 `Player.Deck` 上，因此**无需额外改动**：

| 路径 | 行为 |
|---|---|
| 战斗胜利卡牌奖励 | 各选一张 → 都进共享卡组 |
| 商店买卡 | 金币各自（见 §5.5），谁买谁出钱，卡进共享卡组 |
| 火堆升级 | 各升一张（同一份卡组，各自选牌） |
| 事件加卡 / 删卡 | 共享决策 + 作用于共享卡组 |
| 宝箱 / 精英遗物 | 遗物不镜像，各拿各的（见 §3.6） |

### 2.2 需要注意的点

1. **并发写入**：原版是「各自独立的卡组」，现在两处写入落到同一个 `CardPile`。
   `CardPile.AddInternal` 是无状态追加，不会冲突；但要确认奖励界面的
   「卡组已变化」类 UI 刷新不会读到中间态
2. **卡组查看界面**：两人看到的是同一份（正确）。要确认没有「这是对手的卡组」这类分支
   造成显示异常
3. **`RunState.AddCard(card, owner)` 会给卡设 owner**，而 `CardModel.Owner` 有主不可改。
   奖励生成的卡是**新实例**，不会冲突；有冲突风险的是读档路径（同一张卡被两个玩家各设一次
   owner）——由 §1.3 的引导期方案与 §1.6 的回声卡组写空一起规避

### 2.3 可选变体（默认不做）

若希望更贴近「一个角色」的感觉，可以改成**共享奖励池**：一次战斗只弹一份奖励，
任一人选择都对全队生效（另一人只能看着，或做成双人确认）。

代价：卡组成长速度减半，与双人缩放的敌人脱节，需要同步下调敌人血量缩放
（改 `MultiplayerScalingModel` 对本角色生效的路径），复杂度明显上升。
**建议先按 2.1 做，实测手感后再决定是否引入。**

---

## 3. 状态同步方案（血量 / 格挡 / 状态 / 能力 / 遗物）

### 3.1 三条纪律

镜像必须是**纯函数**：不读 `LocalContext`、不用本地时间、不消费 RNG、
没有「只在某一边执行」的分支。校验和是逐 creature、逐牌堆比对的，
任何一处不对称都会**当场把客户端踢下线**（不是慢慢跑偏）。

### 3.2 生命

- 只有一个池，**不是双倍**。谁挨打都掉同一个数
- 收口点：`Creature.CurrentHp` / `MaxHp` 的 private setter。两者都带
  `if (旧值 != 新值)` 变化判断，所以双向镜像**天然收敛、不会无限递归**，
  一个后置补丁即可
- 注意：`Creature` 构造函数是**直接写字段**（不走 setter）的，所以 `Arm()` 时
  要主动对齐一次；读档后同样（存档里 `current_hp` 是每人一份）

### 3.3 格挡

**先把「双倍」这个说法换掉：格挡从头到尾只有一份。**

- 原版卡**一行都不用改**：`CreatureCmd.GainBlock` → `Hook` → `GainBlockInternal`
  → `Block = ...`，写的是「谁的 creature」不重要，只要两人的格挡是同一个逻辑值，
  两人打防御牌就都进同一个池
- 「两人同时起相同护甲」不是要解决的问题：p1 打防御 +5、p2 打防御 +5 → 池子 +10。
  这**正好等于**原版双人局两人合计 10 点格挡，而敌人打的也是同一个池。
  数值上自洽，不需要发明规则
- 收口点：`Creature.Block` 的 setter（全工程只有这里写 `_block`）

三处必须拍板的交互：

| 交互 | 问题 | 建议 |
|---|---|---|
| 额外回合 | `StartTurn` 对额外回合只让特定玩家参与，而 `AfterTurnStart` 会 `ClearBlock()` → 共享格挡被清空，把另一方攒的也清掉 | 明确二选一：接受「身体重新开始」，或额外回合不清共享格挡 |
| `TurnNumber` 独立 | 两人的回合数是独立的（本体刻意如此，`AfterTurnStart` 的 turn-1 特例就靠它判断），清格挡的时机在特殊时序下不一定对齐 | 实测，必要时把清除条件改成只看锚点 |
| 跨玩家判定 | 原版有「目标没有格挡就…」的效果，共享池下 p1 的格挡会影响针对 p2 的判定 | 语义上是对的（本来一个池），在设计上认下来即可 |

### 3.4 能力（powers）

**关键事实**：本体钩子是**全量广播**——`Hook.AfterCardPlayed` 就是
`foreach (AbstractModel model in combatState.IterateHookListeners())`，
每个能力都会收到战斗里发生的**每一个**事件，然后用 owner 判断自己要不要管。

```csharp
// PanachePower：每打出 5 张牌造成一次伤害
public override async Task AfterCardPlayed(PlayerChoiceContext choiceContext, CardPlay cardPlay)
{
    if (cardPlay.Card.Owner != base.Owner.Player) return;   // ← 全部的开关就是这一行
    ...
}
```

于是有两条路，**都是零成本**的：

| 策略 | 做法 | 效果 | 适用 |
|---|---|---|---|
| **镜像两份**（默认） | 两个 creature 各挂一份 | 每份只数自己的牌、只对自己的回合生效 → **完全等价原版双人局**（两个人各买了一份） | 绝大多数能力 |
| **单份 + 放宽 owner 过滤** | 只挂锚点，把「我的牌」放宽成「队里任何人的牌」 | 一份就数两个人的动作，**效果只出一次** | 需求上「要合并计数、只生效一次」的能力 |

第二条有个反直觉的关键点：**单份不需要任何事件转发**。钩子本来就广播，
那份能力已经收到另一名玩家打出的牌了，只是被 owner 判断挡掉，放开那一行即可。

**分类判据**：问一句「这东西在原版双人局里，是两个玩家各买一份，还是全队共享一份？」

| 类别 | 例子 | 做法 |
|---|---|---|
| 数值/叠加型 | 力量、敏捷 | 镜像（两人各自结算） |
| 每回合触发型 | 每回合开始获得格挡、每回合结束失血 | 镜像（各在自己的回合触发一次） |
| 事件计数型 | Panache、After Image（每打出一张牌就计数/触发） | 二选一：镜像＝各数各的（等价原版）；单份+放宽＝合并计数只生效一次 |
| 全局唯一/互斥型 | 依赖「世界上只有一份」的能力 | 只挂锚点，另一侧不挂（或挂纯显示副本） |

实现上需要一张**每能力策略表**（镜像 / 单份放宽 / 单份原样 / 只锚点）。
这是唯一躲不掉的「逐项写」的地方，因为语义本来就是逐能力的。

写入口收口（都是 public）：

```
PowerModel.ApplyInternal     （施加）
PowerModel.RemoveInternal    （移除）
PowerModel.SetAmount         （改数值；内部回调 Creature.InvokePowerModified）
```

补充说明：

- 镜像意味着身体上会有**两份同类型 power**，各自挂在自己的 creature 上。
  「回合开始/结束」类会在两人各自的回合里各触发一次 → **一份状态、两份收益，
  持续时间也两倍速消耗**。这在数值上自洽，但个别只想触发一次的 power 要走白名单
- 「另一侧挂纯显示副本」需要按实例拦截 `AbstractModel.ShouldReceiveCombatHooks` 的 getter，
  属于脏活；更省事的做法是接受状态图标只出现在一侧

### 3.5 死亡与复活（最高危）

本体有一整套死亡保护与复活：防死钩子、`Player.ReviveBeforeCombatEnd`
（多人局里「还有队友活着就把你 1 血拉起来」）、「死亡时复活」类遗物。

**问题**：共享池归零是两人同时死，多人局那套复活逻辑走不到；而复活类效果若被两人各带一份，
会出现「两条命」且复活血量互相覆盖（谁先复活，另一个的血量同步过去就把它盖掉）。

**方案**：

1. 死亡/复活类效果一律**单份**（不镜像、不双份），把「共享池归零」统一成一个死亡入口
2. 明确 **团灭即终局**，并确认 `CheckWinCondition → PendingLoss` 的判定路径能正常走完
3. 兜底：任何时刻把较高血量同步到较低的一方，防止出现「一死一活」的不可能态

### 3.6 遗物（本次决定：不镜像）

**遗物保持各自独立**，也就是等价原版双人局：每人一条遗物栏，各自从自己的奖励里拿，
每件遗物只影响持有者的行动。

这样做的好处很直接：

- **不需要任何克隆逻辑**。镜像遗物原本要面对 `RelicModel` 单一 `Owner`、
  `AbstractModel.MutableClone()` 的浅拷贝陷阱（只有模型自己覆写了 `DeepCloneFields`
  的字段才会深拷）、部分遗物不能有第二份等问题，现在全部绕开
- 钩子数量与原版双人局一致，不会出现「同一件遗物的两个副本各触发一次」的语义畸形
- `RelicCmd.Obtain` 是唯一获取收口，**不需要打补丁**

要注意的三点：

1. **效果的落点会跨玩家**。遗物给的数值若写进共享池（加格挡、加血、加最大生命），
   依然会惠及双方；而遗物给的「你的攻击 +X」只作用于持有者自己的出牌。
   这是自然的，但要在设计上认下来并在文案里说明
2. **读「你的牌堆 / 你的血量」的遗物**，在共享牌库下语义会变（看到的是同一份），
   需要逐个核对
3. **战斗开始类遗物**（如「战斗开始时抽 2 张」）两人各自触发 → 共抽 4 张，
   与共享抽牌堆叠加后消耗也翻倍，等价原版双人局

移除遗物的路径（`RelicCmd.Remove`）同样**不需要处理**。

---

## 4. 卡牌问题的解决方案

这一节解决「同一张牌在两个玩家之间流动」的全部问题，是共享牌库的必要配套。

### 4.1 问题：`Owner` 是单一引用，且不可改

```csharp
public Player Owner
{
    get { AssertMutable(); return _owner; }
    set
    {
        AssertMutable();
        if (_owner != null && value != null)
            throw new InvalidOperationException("Card " + base.Id.Entry + " already has an owner.");
        _owner = value;
    }
}
```

而战斗内的牌是 `PopulateCombatState` 从主卡组克隆来的，**克隆保留原 owner**
（`CardModel.AfterCloned` 重置了 `DeckVersion`、`CurrentTarget`、各种事件，
但**没有重置 `_owner`**）。

所以共享抽牌堆里会同时存在 owner 是 p1 和 owner 是 p2 的牌。后果是致命的：

| 现象 | 原因 |
|---|---|
| 牌在手里却算出 `Pile == null` | `CardModel.Pile => _owner?.Piles.FirstOrDefault(p => p.Cards.Contains(this))` 只在该卡自己的 owner 的堆里找自己 |
| p2 打牌扣的是 **p1 的能量**、伤害算在 p1 身上 | `Owner.PlayerCombatState.Energy` / `Owner.Creature` |
| `Add` 里的校验全错 | owner 为空抛异常、`creature.IsDead` 判定、`oldPile` 记录都基于 `card.Owner` |
| UI 不认「这是我的牌」 | `LocalContext.IsMine(card)` |

### 4.2 方案：进谁的手牌，owner 就改成谁

引擎自己就留了这条路，本体「把牌交给另一个玩家」用的就是它：

```csharp
card.RemoveFromCurrentPile(silent: true);
card.GiveToAnotherPlayer(player);          // public，直接改 _owner
await CardPileCmd.Add(cards, pile, position, clonedBy,
                      skipVisuals: true,
                      isChangingOwners: true);   // 跳过"重新进入战斗"，避免重复触发 AfterCardEnteredCombat
```

**规则一句话：牌进谁的手牌，owner 就改成谁。**

补丁点建议放在「牌进入 `Hand` 堆」的统一入口上。`Hand` 没有被重定向，
所以从目标堆反查「这是谁的堆」是唯一且无歧义的。两个候选落点，实现时二选一并实测覆盖范围：

| 落点 | 优点 | 风险 |
|---|---|---|
| `CardPileCmd.DrawInternal`（有 `player` 参数） | 直接拿到玩家，最明确 | 只覆盖「抽牌」这一条路径 |
| `CardPileCmd.Add`（判断目标堆 Type == Hand） | 覆盖所有进手牌的路径（含从弃牌堆取回等） | 需要从 pile 反查玩家；且 `Add` 内部有「同批牌 owner 必须一致」的校验 |

### 4.3 连带要处理的卡牌问题

| 问题 | 说明 | 处理 |
|---|---|---|
| 批量进手牌 | `CardPileCmd.Add` 里 `if (player == card3.Owner) continue; throw "Tried to add cards with different owners to the same pile!"` | 进堆前统一 owner，或逐张提交；必须实测覆盖 |
| 「从弃牌堆取回一张牌」类效果 | 走的是同一条进手牌通道，但源堆是共享弃牌堆 | 与 4.2 同一处补丁覆盖 |
| 生成卡（Shiv / 攻击药水等） | 生成时机已经带正确 owner | 不需要处理，但要确认生成路径不经过「共享堆 → 手牌」 |
| `CreateDupe(newOwner)` / 复制效果 | 产生的新实例自带新 owner | 不需要处理 |
| `DeckVersion` 与战斗中永久升级 | 战斗内 clone 通过 `DeckVersion` 指回主卡组那张牌；升级写的是主卡组版本 | 共享卡组下语义一致，两人升级同一张主卡组牌即可 |
| **递牌（Handoff）** | 把手上任意一张牌移到队友手牌 | 这条通道是官方支持的，**白送一个玩法** |
| 卡牌视觉归属 | 牌在共享堆里时 `NCard` 节点挂在谁的 creature 下、飞牌动画飞向谁 | 表现层单独接管：按「目标手牌属于谁」决定飞行终点 |

---

## 5. 其他需要的功能

### 5.1 选人握手（两人同时选择才行）

本体缺的只有这一环，但现成的表现可以复用：

- `NCharacterSelectScreen.SelectCharacter` 遇到 `charSelectButton.IsLocked` 会
  **禁用起程按钮**并显示锁定文案（`CHARACTER_SELECT.locked.title` + `GetUnlockText()`）
  ——行为正是我们要的「条件不满足就不让走」
- 玩家的角色选择是**全网同步**的（`StartRunLobby` + `LobbyPlayerChangedCharacterMessage`），
  且 `IStartRunLobbyListener.PlayerChanged` 可以用来重算按钮状态
- 所以「两人是否都选了这个角色」**直接从大厅状态就能读出来**，不需要自定义网络消息；
  RitsuLib 的 `RunSavedData` 只是备选

建议流程（双向承诺）：

1. 我点这个角色 → 本地按钮进入「等待共鸣…」状态（`IsLocked` 返回 true → 起程被禁用）
2. 对方的大厅面板上该角色出现「成对」标记
3. 两人都选了 → 双方按钮同时点亮、起程允许；角色信息面板显示共享规则
4. 一方改选别的角色 → 双方立刻回到「等待」状态
5. 单人局 / 3-4 人局：灰掉，文案走锁定分支（「需要两名玩家的共鸣」）

要额外处理的边界：

| 边界 | 处理 |
|---|---|
| 随机角色 | `RandomCharacter` 在 `BeginRunLocally` 里解析；若解析成该角色会破坏配对 → 排除该角色出随机池，或在解析后重掷 |
| 有人中途改选 | 靠 `PlayerChanged` 回滚状态 |
| 有人取消准备 | 同上 |

### 5.2 配对关系与 Arm

- 配对由 `RunState.Players` 顺序推导，**不需要额外数据**，重连/读档后自然一致
- `Arm()` 的时机：run 建成之后、任何卡组写入之前（见 §1.3）
- 若需要跨存档记住「本局是共享角色局」，用 RitsuLib 的
  `RitsuLibFramework.GetRunSavedDataStore(modId)` 注册一个 run 级槽位；
  `AttachedState` / `SavedAttachedState` 适合挂在具体对象上的运行时状态

### 5.3 表现层

| 项目 | 问题 | 方案 |
|---|---|---|
| 血条 | 必须有两个 creature → 会画两条血条 | 用 RitsuLib 的 `Combat/HealthBars` 模块 graft 成一条 |
| 站位 | 两个 creature 会并排站 | 用 `Creature.SlotName` 做「一体两面」：主体 + 半透明重影，而不是两个并排的人 |
| 敌人意图 | 意图会指向其中一个 creature，玩家看不出等价 | 统一指向锚点的 creature，并在 UI 上标明「打谁都一样」 |
| 状态图标 | 镜像两份 → 两排同样的图标 | 只显示一份并标注为共享 |
| 受伤反馈 | 敌人打回声的 creature 时，锚点玩家看到「血掉了但我没被打」 | 共享血条做统一的受击抖动/飘字，飘字标明「共享」 |
| 卡牌飞行 | 见 §4.3 | 按目标手牌归属决定动画终点 |
| 手牌 / 能量 | 各自独立 | 零改动 |

### 5.4 存档与重连

- 配对关系由 `Players` 顺序推导 → 无需持久化
- **重连**：存档里没有战斗状态，所以重连要么重进当前房间、要么由主机另想办法。
  不管走哪条，**「重新填充共享抽牌堆只跑一次」这条规则必须同样作用于重连路径**，
  并且要幂等（例如用 run 级标记记录「本场战斗已填充过」）。这是最需要提前实测的一条
- 一人中途退出：建议**直接结束跑局**，比做「残废模式」省事，也符合「同生共死」的设定

### 5.5 金币（待拍板）

建议**保持各自**（原版行为，零改动、避免并发扣钱和重复购买的问题）。
若想更贴近「一个身体、一个钱包」，可以改成共享：需要把金币的增减与商店扣费都收敛到锚点，
并处理「两人同时购买同一件商品」的并发。默认不做。

### 5.6 自检与调试

建议加一个可开关的自检：在每次 `GenerateChecksum` 之前打印两个 creature 的
`CurrentHp / MaxHp / Block / Powers`，以及两个玩家的四堆数量。
理由是校验和只告诉你「分叉了」并立刻踢人，但不告诉你**哪一步先错**；
有这份日志就能一眼看出是格挡、血量还是状态先分歧。

### 5.7 内容与本地化清单（骨架）

- 角色 / 卡池 / 遗物池 / 药水池各一个注册类
- 本地化表名用复数，放在 `res://together/localization/zhs/`：
  `cards.json` / `characters.json` / `relics.json` / `powers.json` /
  `card_keywords.json` / `static_hover_tips.json`
- 美术未就绪时用占位路径（`Const.Paths.xxx`），只会刷 "Missing resource path" 警告并回退

---

## 6. 可能出现的 Bug 与解决方法

按严重程度分档，前两档是必须先解决的。

### 第一档：会直接崩 / 立刻软锁

| # | 症状 | 原因（已核实） | 解决 |
|---|---|---|---|
| 1 | 开新局或读档时抛 `Inventory is already populated` / `Deck has already been populated` | 引导期守卫：`IsInventoryPopulated` 把「卡组非空」当作已填充，而玩家是逐个 created/loaded 的 | §1.3 分两期：引导期不重定向，run 建成后 `Arm()` |
| 2 | 抛 `Card xxx already has an owner` | `RunState.AddCard(card, owner)` 会给卡设 owner，而 `Owner` setter 有主不可改；共享卡组会让同一批卡被设两次 | 引导期不共享 + 回声卡组序列化成空（§1.6）+ owner 语义统一由「进谁手牌归谁」接管（§4.2） |
| 3 | 批量加牌抛 `Tried to add cards with different owners to the same pile` | `CardPileCmd.Add` 的显式校验 | 进堆前统一 owner，或逐张提交；需实测所有批量路径 |
| 4 | 客户端被踢下线（`StateDivergence`） | 校验和逐 creature、逐牌堆比对，任何不对称立刻断线 | §3.1 三条纪律；配 §5.6 自检日志定位先错的字段 |
| 5 | 抽牌堆抽空后该玩家再也抽不到牌（软锁） | `ShuffleIfNecessary` 只检查「该玩家自己的弃牌堆」 | 四堆一起共享（推荐），或接管洗牌逻辑 |

### 第二档：状态分叉 / 语义畸形

| # | 症状 | 原因 | 解决 |
|---|---|---|---|
| 6 | 一次洗牌被同一件遗物的两个副本各改一次 | `Hook.ModifyShuffleOrder` 是广播；遗物若镜像两份就会各改一次 | **遗物不镜像后自动消失**；若将来恢复镜像，需把洗牌钩子的 player 参数固定为锚点 |
| 7 | 额外回合把共享格挡清空、另一方状态不明 | `StartTurn` 的额外回合只让特定玩家参与，而 `AfterTurnStart` 会 `ClearBlock()` | 额外回合不清共享格挡；实测「另一方此时能否出牌」（牵涉 `PlayersReadyToEndTurn` / `PlayerActionsDisabled`） |
| 8 | 出现「两条命」、复活血量互相覆盖 | 死亡/复活类效果被两人各带一份 | §3.5：单份 + 统一死亡入口 + 团灭即终局 + 高值覆盖低值兜底 |
| 9 | 重连后抽牌堆翻倍或错乱 | 存档不含战斗状态，重连要重建；若两处都跑 `PopulateCombatState` 就翻倍 | 把「只锚点填充」同样作用于重连路径，并用幂等标记 |
| 10 | 某次镜像 bug 之后每次都从错误状态起步 | 存档里血量等是每人一份，错误状态被固化 | `Arm()`（含读档后）用锚点强制覆盖回声 |
| 11 | 整局后续随机内容（奖励、敌人行动、地图）与预期不同 | 多余的 RNG 消费会平移全局 RNG 序列；两端仍一致所以不断线，但内容变了 | 镜像逻辑零 RNG 消费；洗牌只发生一次；加日志比对 RNG 消费次数 |

### 第三档：长尾与表现

| # | 症状 | 原因 | 解决 |
|---|---|---|---|
| 12 | 敌人意图指向谁看不出、目标选择类效果语义变化 | 敌人从两个 creature 里挑目标 | 表现层统一指向锚点并说明「打谁都一样」；目标选择类能力逐个核对 |
| 13 | 站位/掩护类能力语义未定义 | `TankPower` / `InterceptPower` / `SurroundedPower` / `FlankingPower` / `CoveredPower` 这批依赖「多目标 / 掩护 / 埋伏」 | 走白名单逐项确认，必要时禁用 |
| 14 | 需要「只触发一次」的能力触发了两次 | 镜像两份 → 各自触发 | §3.4 的单份 + 放宽 owner 过滤 |
| 15 | 状态图标出现两排、受伤反馈错位 | 两个 creature 各自的表现 | §5.3 |
| 16 | 商店/奖励并发时对方界面不刷新、商品被拿走 | 共享对象 + 原版按玩家发的同步消息 | 共享资源操作串行化，或操作后主动刷新；逐条走查商店/火堆/宝箱 |
| 17 | 卡牌飞到错误的人身上 | 牌的视觉归属依赖 owner，而 owner 在手牌入口才改 | 表现层按目标手牌归属决定动画终点 |

### 第四档：平衡

| # | 问题 | 说明 | 建议 |
|---|---|---|---|
| 18 | 一条血池 vs 原版两条 | 敌人按两名玩家缩放，我方只有一个池 | 起始血量给到单人角色的 ~1.25 倍起步（90~110 区间），实测调 |
| 19 | 爆发伤是短板 | 两个敌人同回合都打身体，伤害在一个池里叠加（原版是分摊到两个池） | 补偿项：格挡不浪费、两人都能出防御牌；可给起始遗物一个「过载保护」（如回合开始两人能量相等就给格挡） |
| 20 | 每轮摸 10 张，牌组循环 2 倍速 | 20~30 张的牌组第三轮就洗牌 | 起始抽牌数（5/5 或 4/4）留成可调项，早测 |
| 21 | 消耗牌价值变化 | 循环更快 → 消耗牌更快退出循环 | 设计旋钮，不是 bug |

---

## 7. 开发顺序（建议）

先把风险最高的部分验证掉，再做内容。

| 阶段 | 内容 | 验证点 |
|---|---|---|
| M0 | 双向选人握手 + 配对/Arm + 单人局与 3-4 人局禁用 | 能进局不崩（Bug #1、#2） |
| M1 | 共享主卡组 + 四堆重定向 + owner 交接 | 一场完整战斗：抽/弃/洗牌正常，校验和不断线（#3~#5） |
| M2 | 血量/格挡/能力镜像 + 自检日志 | 一场战斗打满，受击/治疗/状态都对（#4、#11） |
| M3 | 死亡语义 | 打死一次：两人同时死、GameOver 正常（#8） |
| M4 | 存档 / 读档 / 断线重连 | 退出继续 + 中途重连（#9、#10） |
| M5 | 非战斗流程走查 | 商店 / 火堆 / 宝箱 / 事件各走一遍（#16） |
| M6 | 能力白名单逐项收敛 | 每个 power 过一遍分类表（#13、#14） |
| M7 | 表现层 | 血条合一、站位、受击反馈、意图指向（#12、#15、#17） |
| M8 | 数值调平 + 内容（卡池 / 遗物池 / 美术） | #18~#21 |

---

## 8. 尚未决定的问题

1. 起始血量上限（建议 90~110 起步）
2. 每人起始抽牌数（5 张 or 4 张）
3. 额外回合的格挡处理（清 or 不清）
4. 金币是否共享（建议先各自）
5. 是否允许将来支持 4 人局里的两对配对（当前只做 2 人局）
6. 每日挑战 / 成就 / 排行榜是否纳入（共享血池的成绩是否该进榜）

---

## 9. 联机实测记录（2026-09-15）

> 环境：本机双人（DualRoleAdventure 回环主机），两人都选共享角色，进阶 10。
> 结论：**M0–M3 + 存读档 + 非战斗流程全部验证通过**。以下六条是实测中真实踩到、并已落成代码约束的坑。

### 9.1 已通过的验证项

| 项目 | 结果 |
|---|---|
| M0 选人门控（单人局锁定 / 两人都选才能起程） | ✅ |
| M1 共享卡组 + 共享抽/弃/消耗/打出堆 + 各自手牌与能量 | ✅ |
| M2 血量 / 格挡 / 能力镜像 | ✅ |
| M3 死亡（同生共死）与复活 | ✅ |
| 存档 / 读档 / 断线重连 | ✅ |
| 非战斗流程（商店 / 事件 / 火堆 / 宝箱） | ✅ |

### 9.2 实测新增的六条硬约束

**① 激活时机必须晚于 RunState 构造完成。**
`RunState.CreateShared` 的顺序是"先设某个玩家的 `RunState`，再遍历它的牌组给每张卡设 owner"。
若在"拿到 RunState 的瞬间"就激活共享（懒激活），第二个玩家那次遍历会读到**被重定向的卡组**，
同一张牌被设两次 owner → `CardModel.Owner` 的 setter 抛 `already has an owner` → 开局中断（表现为黑屏）。
⇒ 只在跑局工厂（`RunState.CreateForNewRun` / `FromSerializable`）的 **Postfix** 里激活。

**② 补丁必须用类级 `[HarmonyPatch]`，并且逐个类安装。**
方法级 `[HarmonyPatch]` 在当前 Harmony 构建下**不被 `PatchAll` 识别**（实测日志里 `Harmony patched 0 method(s)`），
而且**不报错** —— 整套功能静默失效，编译、初始化、日志全部正常。
另外 `PatchAll` **不是事务性的**：某个补丁类抛异常（例如参数索引写错）会中断后续安装，
留下"一半功能正常、一半消失"的状态（实测表现：能力共享还在、卡组共享没了）。
⇒ 类级特性 + `harmony.CreateClassProcessor(type).Patch()` 逐个安装，并把 `applied/failed` 打进启动日志。

**③ 归属规则必须成对：在手牌里归手牌主人，离开手牌归锚点。**
只做"进手牌改 owner"会让共享的弃牌堆／抽牌堆里出现**混合归属**，而本体批量 `CardPileCmd.Add`
有一条硬校验："同一次调用里所有牌 owner 必须一致"。
洗牌正好走批量 Add（把弃牌堆混进抽牌堆），于是抛
`Tried to add cards with different owners to the same pile!` —— **回合循环直接终止、战斗卡死**。
⇒ 归一化必须落在 `CardPile.AddInternal` 的 **Postfix**（同步、且是入堆的唯一通道），
并且**刻意不碰 Play 堆**：`OnPlayWrapper` 是"先放进 Play 堆、之后才用 `Owner` 计算打完去哪"，
中途改 owner 会让它判定成换玩家、多走一次交牌分支。

**④ 不能"先改 owner 再让 Add 摘出原堆"：`CardModel.Pile` 是按 owner 反查的。**
`Pile => _owner?.Piles.FirstOrDefault(p => p.Cards.Contains(this))`。
若在 `Add` 的 Prefix 里改 owner，`Add` 内部随后靠 `Pile` 摘除时会找不到原堆 → **摘除静默失败**
→ 牌同时留在两处（幽灵卡 / 多出卡牌 / 不能正确弃牌 / 计数不刷新）。
⇒ 时机见 ③；另外给 `Pile` 加"自己堆里找不到就去另一半的堆里找"的兜底，
并给手牌节点加一道幂等清理（`NPlayerHand.GetCardHolder` 确认存在才 `Remove`）。

**⑤ 卡池必须覆盖 3 类型 × 3 稀有度。**

- 战后**卡牌奖励**要求 Common / Uncommon / Rare 三个桶都非空，否则抛 `couldn't generate a valid rarity` → 战斗胜利不给奖励。
- 商店生成库存要求**每种类型**（Attack / Skill / Power）都有牌，否则抛
  `Can't generate valid rarity for merchant card type Power`，而这条异常会让 `MoveToMapCoordAction`
  带异常结束 → **进房间卡死**。

⇒ 模板卡池最少要有 3×3 的覆盖（起始 Basic 卡不算数）。

**⑥ 只重定向 getter 不够，必须替换字段本身。**
界面（顶部卡组按钮、卡组界面、战斗牌堆控件）会在初始化时抓一次 `PileType.X.GetPile(player)`，
把 `CardPile` 引用**存进自己的字段**，之后只看那个引用。
只改 getter 的话这份"旧引用"永远是回声自己那份（实测表现："p1 显示 11 张、p2 只有 9 张初始卡"）。
⇒ 直接替换 `<Deck>k__BackingField` 以及四个战斗牌堆的 backing field（`Hand` 除外）。

### 9.3 仍未解决

| 问题 | 性质 |
|---|---|
| 抽/弃牌堆计数不能即时刷新 | 纯 UI：控件缓存了牌堆引用、只在控制权切换时才重建。**数据层已验证正确**。 |
| `All rewards have been taken, but the rewards set is not complete on the backend!` | 奖励后端的抱怨，尚未观察到对应的可见症状（疑与多角色 mod 的汇总奖励流程有关）。 |
| 战斗结束瞬间的 `PlayCardAction … finished execution, but was in state Canceled!` | 出牌动作被暂停/取消的状态提示，实测出牌表现正常。 |

### 9.4 排查期工具（保留）

- 所有 `[together][diag]` 诊断日志统一由 `SelfCheck.Write` 输出，**默认静默**；
  需要排查时在启动游戏前设环境变量 `TOGETHER_SELFCHECK=1`。
- 启动日志会打印 `patch classes applied=N failed=M; Harmony patched K method(s)`：
  `failed` 不为 0、或 `applied` 数量与补丁类数量对不上，就说明有补丁没装上。
- 本机双人用的是回环主机（`NetGameType.Host`、无真实对端），**校验和不会报分叉**，
  所以本地测试时上面那份自检日志就是唯一的分叉探测器。
